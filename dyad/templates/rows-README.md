# d-work rows (canonical ledger, Rule-16)

One file per d-work, `<id>.md`, `key: value` lines: id, title, opened, state, disposed, refs.
Render the table with `python3 dyad/scripts/package.py ledger`; never hand-edit a row (use `package.py dwork`).
