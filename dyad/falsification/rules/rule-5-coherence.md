# Falsification record — Rule-5 (rule-set coherence)

**Claim (operator, 2026-09-12):** the set {Rule-1, Rule-2, Rule-3, Rule-4, Rule-5} is coherent
in relationship and orthogonal in concerns.

| # | Attack | Result | Survivor |
|---|--------|--------|----------|
| 1 | Enumerating the set means Rule-6 forces an edit to Rule-5 — referrer drift (cf. G5). | Confirmed | Target is "the set of Rules", never enumerated. |
| 2 | Self-inclusion: Rule-5 judges a set that contains it. | Survives | Same as Rule-4. The sweep below covers Rule-5 itself. |
| 3 | "Coherent" and "orthogonal" are undefined; any claim is unfalsifiable. | Refuted as stated | Defined operationally in the Rule (same-trigger contradiction; resolvable references; single owner; one-way references named in Boundaries). |
| 4 | Not mechanisable — a principle, not a rule. | Survives, scoped | Inference-only, like Rule-2, but with a hard trigger: every rule change carries a pairwise statement. The two prior sweeps were operator-prompted; this makes them mandatory. |
| 5 | Overlap with Rule-4 on the trigger "a Rule is edited". | Survives | Same trigger, disjoint concern (shape vs. relations). Each names the other in Boundaries; ownership is one-sided. |

## Sweep 3 — pairwise, Rules 1–5 (first evidence under the new condition)

| pair | coherent | orthogonal | note |
|---|---|---|---|
| 1–2 | yes | yes | Rule-2 depends on Rule-1 for where mechanisms live; Rule-1 Boundaries name Rule-2. |
| 1–3 | yes | yes | Rule-3 mechanisms are infra-zone (Rule-1); Rule-3 Boundaries name Rule-1. |
| 1–4 | yes | yes | Rule-4's script is infra-zone; Rule-4 parses the Rule-1 section of CLAUDE.md, a read, not ownership. |
| 1–5 | yes | yes | Rule-1 Boundaries name Rule-5; Rule-5 does not touch paths. |
| 2–3 | yes | yes | Rule-3 declares events; Rule-2 applies by reference (G5); `main-fence.sh` owned by Rule-3, named by Rule-2. |
| 2–4 | yes | yes | Rule-4 checks Rule-2's shape only. |
| 2–5 | yes | yes | Rule-5 sweeps are agent-authored verdicts; finalising one is a Rule-2 event via Done-`Y`. |
| 3–4 | yes | yes | A Rule-4 PR is a d-work like any other. |
| 3–5 | yes | yes | A sweep is a d-work; Rule-5 does not define when work opens or closes. |
| 4–5 | yes | yes | Attack 5. |

No gap found. Disposition: see ledger #50.

## Amendment — d-work #154 (2026-09-14, craft reframe)
New paragraph `## Homes` after Definitions: three homes; this Rule sweeps the core craft's
Rules; a Tended craft sweeps its own by its own form. Block unchanged.
| # | attack | result | survivor |
|---|---|---|---|
| 6 | "kernels core" reuses Rule-14's `kernel` (Rule-6 consistency). | Confirmed | Worded "process kernel of a Rule that splits"; no new term. |
| 7 | The Homes paragraph narrows the sweep before any Rule has left the core craft. | Survives, scoped | Today every Agent Rule is in the core craft, so the sweep is unchanged; #160 is where the set shrinks, with its own record. |

Pairwise: the Homes paragraph scopes Rule-5's sweep; classification stays Rule-4's (Boundaries);
the craft tree stays Rule-11's; no concern moves. Ordered gap (Rule-8/19 paths until #155) noted
in `rule-11-distribution-structure.md`, this amendment. Coherent, orthogonal.

Disposition: see ledger #154.

## Amendment — d-work #160 (2026-09-14, sysarch extraction R-craft-3)
`## Homes` becomes `## Sets` with the final wording (plan #160 D3): System Requirements = the core craft's Agent Rules,
kernels of 11/12/14/16/20 included; System Architecture = `crafts/sysarch/rules/`, Tended, swept by the craft's own form;
Rule-13 transitional (attack 11). Target names one set. Block unchanged (boundaries 5, conditions 3).
| # | attack | result | survivor |
|---|---|---|---|
| 8 | The Target now names one set while two `Set:` lines (Rules 13, 19) still say System Architecture. | Survives, scoped | Transitional: Rule-13 pending #162 is stated in Sets; Rule-19's line dates from #155 and is a finding for #162's sweep. Rule-4 counts, Rule-6 checks unaffected. |
Coherent, orthogonal. Disposition: see ledger #160.
