# Plan #57 — evaluate the need for a minimum sysadmin craft

Base commit of `main`: 771074d. Refs #51–#55 (workstation#224 intakes, which presume host
administration on an enterprise host), Rule-8/18/19 kernels, Rule-11 p7.

## Intent as read
"Minimum `sysadmin` craft to maintain and evolve the host infrastructure": is there a need, and
whose — this system's, or the distribution's? Two readings, both answered:
- R1: does *this* system (the authoring repo, on this workstation) need a sysadmin craft to tend
  its own host?
- R2: does the distribution (`BUNDLE.md`: dyad-operator, sysarch, syseng) need to ship one, so any
  installation can maintain its host and the core's Rules 8, 18, 19 are satisfiable?

## Read so far (read-only, evidence for the audit)
- Here: no `crafts/sysadmin/`, no `workstation-corpus/`. Measured consequences on `main`:
  `agent/references` — "craft sysadmin absent … 13 token(s) unresolved", 5 kinds skipped
  (`changelog.action->ops`, `ops.dwork->row`, `ops.changelog->changelog`, `changelog.event->event`,
  `event.command->command`); 7 `dyad/tests` cases skipped (`needs_sysadmin`); `dyad runbook check` →
  "refused: no craft provides the run-book check". Rules 8, 18, 19 name `crafts/sysadmin/rules/
  {host-mutation,ops-scripts,server-instances}.md` and the guards `sysadmin/{changelog,ops_scripts,
  runbooks,events}`; the core vocabulary references 14 sysadmin terms by craft name.
- Workstation (`pltrinh1122/workstation`, `crafts/sysadmin/`, VERSION 0.1.0, 38 files, `requires:
  dyad-operator>=0.2.0`, `seeds: CHANGELOG.md->workstation-corpus/CHANGELOG.md`): exactly 3 rules,
  4 guards, 4 templates, 1 projector (`project_events.py`), 14 vocabulary terms — the same three
  rules, four guards and fourteen terms the core names. Its `workstation-corpus/rules/` holds only a
  README: host specifics are instance, not craft. Workstation's releases: `v0.1.0`–`v0.3.1` only;
  no `sysadmin-vX.Y.Z` tag or archive exists anywhere — nothing any installation can
  `dyad craft install` from.
- The host this repo runs on is workstation's host (its session runs on this machine); a second
  change log here would split one host's record across two systems.

## Play-book (Rule-3 Plan clause; `dyad/playbooks/craft-instantiation.md`)
Trigger holds: no installed craft here practises host administration. D3' decides: the craft
*exists* as a craft corpus (workstation's, 0.1.0) → import or extend, never author anew. D1, D2,
D5, D6 do not fire. D4 (ledger coupling) fires for R1 in the other direction: this host's d-works
belong to workstation's ledger, an existing system — not a new one, and not a second instance
here. No craft is instantiated in this d-work: the prompt asks for an evaluation.

## Mutation
One agent-zone PR: `agent-corpus/audits/2026-09-17-sysadmin-minimum.md` — the evaluation:
findings with the evidence above; the options (A: move sysadmin's authoring home here, as sysarch
and syseng were moved from the same pre-split monorepo, bundle row, workstation installs it;
B: keep tending at workstation, workstation publishes `sysadmin-vX.Y.Z`, this repo's CI installs it
into the scratch repo, README names it as the host-administration craft; C: install it here as a
registry row and widen Rule-11 p7 to bundle installed crafts); a falsification table; a
recommendation; and the follow-on d-works each option needs, for the Operator to prompt. No Rule,
craft or preference edit in this d-work; nothing sent to workstation until the audit is disposed.

Recommendation as drafted, subject to the audit's falsification: **the minimum craft already
exists** — workstation's sysadmin 0.1.0 *is* the kernel contract of Rules 8/18/19, no larger — so
the need is not authoring but a home and a release. Path A (authored here, shipped in the bundle;
the sysarch/syseng precedent; one front door for the enterprise installs #224 plans). Strongest
counter, path B: a host practice evolves best where a host is, and this repo has none — its guards
can only be fixture-tested here. Reconciliation: that is already true of sysarch and syseng, whose
live use is at workstation; host specifics already live in `workstation-corpus`; and changes arrive
as intakes with evidence, as #224's did. R1's answer is no either way: this system takes no host
record of its own; a finding is elevated that Rule-8 presumes every system has a host — a pure
authoring system on a host another system tends has no rule for where its host actions are recorded.

## Files touched
agent-corpus/audits/2026-09-17-sysadmin-minimum.md; agent-corpus/d-work/{rows,plans,provenance}/57.md

## Falsification
- "The absence is by design (#155 kernels; install, references and tests all tolerate it)" —
  survives, scoped: tolerated is not satisfiable. Rule-8 Conduct requires the change-log row of
  every reversible or destructive host action in *any* system and calls its absence a breach;
  without the craft no installation of the bundle can meet it. The tolerance is for an install in
  progress, not a shipped state.
- "Just publish `sysadmin-v0.1.0` from workstation" — refuted as sufficient on its own: it fixes
  installability, not that the core releases untested against three of its own Rules' content;
  and an enterprise install (#224) fetching a craft from a personal workstation repo is the wrong
  front door. Kept as path B, the honest counter.
- "Author a *new* minimum here, smaller than workstation's" — refuted: workstation's is already
  the minimum (3/4/14 match the core exactly; no host content in it). Two crafts named `sysadmin`
  would collide on install (Rule-11 p2 refuses an authored tree).
- Presence: `web-sysarch` claims 15 47 48 49 50 (all done; the presence-store defect of #32);
  `web-sysarch-adh151` on #15/#32, files under `crafts/syseng/`, `dyad/guards/agent/{rows,
  sessions}.py`, `dyad/scripts/package.py` — no overlap with an audit file. Host actions: none.
