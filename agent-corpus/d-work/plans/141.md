# Plan #141 — sync with remote (this session's checkout, `ds-syseng`)

Base commit of `main`: 1b035b1. Refs: none. This is git housekeeping in the manner of #59 and #140;
#140 is a peer session's sync of its own checkout, not this one.

## Intent as read
"Sync with remote": bring this checkout to `origin` and reconcile any local branch whose work has
already landed.

## Findings, verified before proposing anything
- `git fetch --all --tags --prune` has run. The working tree is clean.
- Two local branches were clean fast-forwards and are reported here, not gated. #59 and #140 set
  the precedent that a clean fast-forward of a tracking branch is not a decision.
  - `main-view` was 9 behind and 0 ahead; it is now at `1b035b1`.
  - The harness-designated `claude/syseng-pydantic-vs-zod-adh151` was also fast-forwarded
    locally to `origin/main`.
- Six local branches are fully merged into `origin/main`, each confirmed with
  `git merge-base --is-ancestor`. All of them are this session's own branches from #108, #110
  and #138:
  - `dwork-108-done` (44cd6e9)
  - `dwork-110-scope-stage2` (ca925b6)
  - `dwork-138-done` (0bfdac8)
  - `dwork-138-impl` (54c4097)
  - `dwork-138-plan` (899b27e)
  - `dwork-138-plan-y` (c443c7d)
- There is nothing to rebase: no local branch is ahead of its remote.

## Mutation — host action, local only, no push
1. Delete the six merged local branches with `git branch -d`, which refuses anything that is not
   fully merged. This is reversible (Rule-8): undo with `git branch <name> <sha>` using the shas
   above, and every one of them is also on `origin`.

Not in this mutation:
- Remote branch deletion. That is visible on the hosting and was not asked for.
- `main-view` and the designated branch are kept.

## Files touched
agent-corpus/d-work/rows/141.md; agent-corpus/d-work/plans/141.md; agent-corpus/d-work/provenance/141.md

## Falsification
| # | Attack | Result | Survivor |
|---|--------|--------|----------|
| 1 | Deleting the branches loses work. | Refuted | Every branch is an ancestor of `origin/main` and also exists on `origin`; `-d` refuses anything else. |
| 2 | "Sync" should also push the designated branch. | Survives, scoped | That is a remote write nobody asked for. It stays local; the stop hook is satisfied only if a push is later needed for real work. |
| 3 | Overlap (Rule-16). | None | #140 syncs a different checkout (`dev-main`'s root). Nothing here touches another session's files. |
