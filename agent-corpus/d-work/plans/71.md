# Plan #71 — syseng 0.1.3 → 0.1.4; bundle 0.7.2

Base commit of `main`: d328cfc. Refs #67 (same shape, two crafts), #61 (sysarch's instance), #15
(the peer d-work whose 10 syseng files landed under an unmoved 0.1.3).

## Intent as read
Workstation #245 (defect, evidence complete): v0.6.0's `syseng-0.1.3.tar.gz` (`10b2f93b…`, from
`cfc1d55`, where `syseng-v0.1.3` still peels) and v0.7.1's (`dd4f3783…`, built from `321b471`)
differ in 10 files. Verified here: `git diff --stat syseng-v0.1.3..main -- crafts/syseng/` = 10
files, 279+/20−; every other craft is clean against its own tag. #67 checked only the two crafts
workstation had named; a sweep of all four would have caught this.

## Mutation
- Craft zone, one PR: `crafts/syseng/VERSION` 0.1.3 → 0.1.4 (patch; #15 added guard data files
  and rule text, no new Rule-owned surface from this fix's vantage — the #67 reasoning).
- Infra zone, one PR: `BUNDLE.md` `syseng | 0.1.4`, `version: 0.7.1 → 0.7.2`.
- Infra depends on craft; pushed `--no-verify` against a throwaway merge verified with
  `check --evidence`; merged after the Done-`Y`, in order, *after* `dwork state 71 done`
  (Rule-3 Completion, the #68 bullet).
- Releases, one `Y` each after Done-`Y`: `syseng-v0.1.4`, `v0.7.2`. Then a message to workstation
  (its syseng is the older tree; v0.7.2 resolves which "0.1.3" was real: neither — 0.1.4 is).
- Not here: the guard (next d-work).

## Files touched
crafts/syseng/VERSION; BUNDLE.md; agent-corpus/d-work/{rows,plans,provenance}/71.md

## Falsification
- "Re-cut v0.7.1's syseng asset from cfc1d55 instead" — refuted: v0.7.1's BUNDLE.sha256 is
  published and workstation has verified against it; replacing an asset under a published tag is
  the moving-tag problem in another form. Forward only.
- "Minor, not patch (#15 added files)" — same as #67 attack 1: the content already shipped as
  "0.1.3" twice; a patch says "content corrected under the same guarantees". The guard d-work is
  where the bump-size question gets a rule if it needs one.
- Presence: `web-sysarch-adh151` names `crafts/syseng/guards/…`, `crafts/syseng/rules/…`, tests —
  #15's files, that d-work now done; no overlap with `VERSION` or `BUNDLE.md`. Host actions: none.
