# Plan #109 — sync with #107/#108, close the drift-guard gap it left, release

Base commit of `main`: 771c554. Refs #108 (the d-work whose content this releases).

## Intent as read
"Sync and make new release" — a concurrent session (per Rule-16; four presence files checked,
`dev-main`, `web-sysadmin-scope`, `web-sysarch-adh151`, `web-sysarch`, all stale and none
declaring `dyad/VERSION` or `BUNDLE.md`; this session's own `dyad session touch` run first) landed
#107 (profiling, no mutation) and #108 Stage 1 (`dyad/scripts/package.py`'s `evidence_sha256`
extraction, `dyad/tests/test_package.py` rewritten to unit-test it directly — both d-works already
`done`, own Done-`Y`s recorded, PRs #90/#91/#92 already merged) while this session was mid-#106.

`main` is currently red: `FAIL [infra/bundle] 'dyad-operator': dyad/ differs from tag
dyad-operator-v0.8.2 (2 file(s)) while VERSION is still 0.8.2 — bump VERSION` — #108's own PRs
passed `check --pr` (transaction-scoped guards only, never `infra/bundle`'s full version-match) but
never a full `check --guards`, so the drift went unnoticed before merge, matching a known gap (this
session's own earlier error/fix log: `check --pr` doesn't run the full guard sweep).

#108's own change is confirmed behavior-neutral (its own plan: "no behavior change to the CLI") —
a patch-level bump.

## Mutation
Agent zone, one PR: `dyad/VERSION` 0.8.2 -> 0.8.3.
Infra zone, second PR after the first merges (referent before referrer): `BUNDLE.md` dyad-operator
row 0.8.2 -> 0.8.3, bundle version 0.10.3 -> 0.10.4.
Release (the new batch form, Rule-11 Ratification events, #106): `dyad-operator-v0.8.3` and bundle
`v0.10.4` together, since both belong to this one d-work and are named individually below before
the question is asked.

## Files touched
`dyad/VERSION`; `BUNDLE.md` (second PR); `agent-corpus/d-work/sessions/unnamed-1bc8a044a4f6.md`
(this session's own presence touch, piggybacked on this plan's ledger commit, Rule-16).

## Falsification
Attack: bumping `dyad/VERSION` for a d-work (#108) this session didn't author is presumptuous —
the drift might be intentional, left for #108's own session to close. Result: refuted — the drift
guard is unconditional (Rule-11 property 4's converse; no "intentional drift" exemption exists),
`main` is red for every session until it closes, and the Operator's own prompt ("sync and make new
release") directly asks for this, not a rewrite of #108's own content.

Attack: 0.8.3 might be the wrong bump size (minor, not patch) if `evidence_sha256`'s extraction
counts as new public surface. Result: survives, scoped — `evidence_sha256` is called only from
within `cmd_evidence()` itself in the current diff (not exposed as a new CLI flag or documented
entrypoint); treated as patch per semver's own convention (internal refactor, no behavior change,
matching #108's own plan's explicit claim). Reversible if disputed: only a number, superseded by
the next tag if wrong, per #106's own newly-settled principle.
