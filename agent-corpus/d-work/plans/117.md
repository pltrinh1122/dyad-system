# Plan #117 — incident mode A: acting before the Operator's Y (9 incidents: merge before Done-Y, mutation before plan-Y, counter-prompt before the plan file)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode A: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode A)
#47, #58, #67 (merge before Done-Y); #18, #22, #35, #92/#103/#104, #110 (before plan-Y); #67 (Y/N before row and plan file)

## Falsification (candidate remediation)
Every remedy so far was conduct (memory note, Rule-3 Completion bullet #68) and each was followed by a recurrence within hours or days. Candidate survivor: the structural fence #113 already shipped (a fork cannot commit, push, merge or ask), plus one mechanical check this child can propose: a merge helper (dyad dwork merge <id>) that refuses unless the row is already done on origin/main. Attack: the Agent can still call gh directly -- refuted only by conduct; the helper narrows the habitual path, not the possible one.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/117-mode-A`): `agent-corpus/audits/2026-09-22-incident-mode-A.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-A.md`
