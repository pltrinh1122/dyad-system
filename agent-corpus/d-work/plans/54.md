# Plan #54 — workstation#224 gap (d): no scheduled/daemon actor model across Rules 3/8/19

Base commit of `main`: 41c6931. Refs workstation-224; siblings #51–#53, #55.

Intake record (Rule-3 Intake), not an authorized plan: the row is `backlog`, opened on the Operator's `Y` (2026-09-16); no mutation is proposed until the Operator prompts for it. Source: workstation `main` `eb9322d`, `agent-corpus/d-work/plans/224.md` (row #224, "Cross-system reports") and `agent-corpus/falsification/224-boundaries.md`, resent verbatim by `dsv0.5.1: workstation` on 2026-09-16; verified by the sender against its installed core (dyad-operator 0.5.0, sysarch 0.1.2, syseng 0.1.2) — line numbers are that install's.

## Intent as read (the sender's observation, verbatim)
`dyad/scripts/runbook.py:53` `RUN_ROLES = ("operator", "agent")` (the event's `role`; `:272` rejects any other); `crafts/sysadmin/guards/changelog.py:31` `ACTORS = ("operator", "agent")` (`:73` rejects any other). `--as` on `dyad runbook run` selects one of those two — it is a flag, not an identity a scheduler could assert without recording a party that did not act. `dyad/rules/RULE-19-server-instances.md:36` (Conditions): "Either party runs a run-book command: it runs through the runner (`dyad runbook run`, …), which records its event" — presumes a human- or Agent-triggered invocation. Rule-3 Scope's subagent clause ("Subagents never … push a branch … or run a reversible or destructive host action") and Rule-8 Conduct ("A host action never runs from a subagent") do not say whether a host timer or a supervised daemon deployed under a plan-Y is a subagent or a World process; nothing names an authorization or a record form for a run no d-work opened. Workstation's architectural reading (stated as inference in its record, attack 4): a systemd timer running a run-book command's native line (not the runner) writes only to journald and leaves no corpus artifact per run; a supervised daemon is World-side like lan-git's own `receiver.py`.

## Intake shape
Enhancement proposal, stated plainly by the sender: no Operator prompt addresses this gap; it arose from workstation's Agent's own analysis while planning its #224. Under Rule-3 Intake this is not an intake — the row exists because this system's Operator opened it (`Y`, 2026-09-16), which the rule permits. The workstation #224 prompt below is *context* the sender supplied, not a prompt for this row, so it is not a provenance entry here (Rule-7 property 4; the row's provenance holds the Operator's own `Y`).

Context, workstation row #224 provenance entry 1 (its Operator, verbatim as relayed): "`workstation` servers and craft, e.g. `lan-git` and `surfacer` and `sysadmin` will need to support onsite enterprise installation before a firewall and it'll need to operate autonomously.  the `lan-git` serve as an enterprise data repository while the `surfacer` will allow navigation of the `lan-git` artifacts.  the operator will have a claude code license to operate; however, some autonomous actions will use API inferencing directly from Anthropic - e.g. classification and extraction of entities from transripts stored on OneDrive. develop a detailed multi-phase plan for my review - decomposing into multiple d-work as necessary." Entry 2: "Y: i want the outcome of this dwork is focused on architectural refactoring and not actual packaging and deployment.  i plan to have a commercial entity produce the deployment packaging and installation."

## Mutation
None proposed until prompted. Receiving Agent's reading at intake: the observation is accurate (two hard-coded two-member actor tuples; Rules 3/8/19 silent on a scheduler); any change is a Rule amendment across three Rules plus the sysadmin craft, and needs an Operator prompt of this system's before planning.

## Files touched
None yet.

## Falsification
Deferred to planning; the intake-shape finding above is the only judgment made here.
