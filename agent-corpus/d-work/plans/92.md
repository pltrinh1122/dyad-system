# Plan #92 — workstation#241: sysadmin `actor` = hands (intake record)

Base commit of `main`: 72bf9e1. Refs workstation-241 (via `dsv0.5.1: workstation` #244 and
`dsv0.3.1: lan-git` #246 reports, 2026-09-18), #58 (the tree moved here at `0d50804`, before
workstation's `f2b9413`).

Intake record (Rule-3 Intake), not an authorized plan: the row is `backlog`, opened on the
Operator's `Y`; no mutation until prompted.

## Intent as read (the senders' observation, verbatim as carried)
`crafts/sysadmin/guards/changelog.py` `ACTORS = ("operator", "agent")` with the comment "the party
whose hands and credential executed the action" — same wording in `rules/host-mutation.md`,
`templates/CHANGELOG.md`, `falsification/rules/host-mutation.md`. The conjunction has no defined
value when hands and credential diverge: an Operator mints an access token and hands it to the
Agent, which runs the command. Workstation d-work #240 (`agent-corpus/falsification/
240-agent-runs-204-h1.md`, attack 7) found the gap; #241 (merged PRs #288, #289, commit `f2b9413`)
resolved it: `actor` is the party whose hands executed the action (who ran the command); a
divergent credential is named in `outcome`. Reproduces: after workstation installed
`sysadmin-v0.1.0`, `dyad craft list` shows `sysadmin 0.1.0 installed 9ff83da2 (modified)` — the
installed tree and the corrected tree differ on exactly this wording (diffed against the release's
`guards/changelog.py`). Cherry-picked on the fresh install there: applies cleanly, 85 tests pass,
no guard logic or FIELDS/ACTORS values moved — wording only.

Operator prompt behind the original fix (workstation #216, quoted by lan-git): "i agree with
keeping provenance for operator performed mutation, e.g. Gitea UI, and agent performed mutation,
e.g. CLI." — context, not a prompt for this row; not a provenance entry here (Rule-7 property 4).

## Mutation (when prompted)
Craft zone: cherry-pick `f2b9413` from `pltrinh1122/workstation` onto `crafts/sysadmin/`
(wording in four files); `VERSION` 0.1.0 → 0.1.1 — the drift guard (#91) requires it on the first
sysadmin content change. Infra: `BUNDLE.md` row + bump. Releases `sysadmin-v0.1.1`, bundle.

## Files touched
None yet.

## Falsification
Intake shape: defect with evidence (path, commit, reproduction, mechanism verified) and an
Operator prompt behind the fix — admissible on either footing.
