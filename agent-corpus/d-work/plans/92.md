# Plan #92 — workstation#241: sysadmin `actor` = hands

Base commit of `main`: 3f3ca32. Re-planned 2026-09-20 — `main` moved past every touched file
since the 72bf9e1 intake (`changelog.py` gained `REFERENCES_CONTRIB` etc. in #100 PR5, `VERSION`
is already 0.1.1 for that unrelated reason), so Rule-15 phase 2 requires a fresh plan-`Y` (this
one) before execution. Refs workstation-241 (via `dsv0.5.1: workstation` #244 and `dsv0.3.1:
lan-git` #246 reports, 2026-09-18), #58 (the tree moved here at `0d50804`, before workstation's
`f2b9413`).

Prompted 2026-09-20 (Operator `Y` to the batched proposal covering #92, #103, #104): implement
now, release as `sysadmin-v0.1.2` — 0.1.1 is already tagged (#100 PR5, unrelated bump) and tags
do not move (Rule-11 property 4).

## Intent as read (the senders' observation, verbatim as carried)
`crafts/sysadmin/guards/changelog.py` `ACTORS = ("operator", "agent")` with the comment "the party
whose hands and credential executed the action" — same wording in `rules/host-mutation.md`,
`templates/CHANGELOG.md`, `falsification/rules/host-mutation.md`. The conjunction has no defined
value when hands and credential diverge: an Operator mints an access token and hands it to the
Agent, which runs the command. Workstation d-work #240 (`agent-corpus/falsification/
240-agent-runs-204-h1.md`, attack 7) found the gap; #241 (merged PRs #288, #289, commit `f2b9413`)
resolved it: `actor` is the party whose hands executed the action (who ran the command); a
divergent credential is named in `outcome`. Reproduces: after workstation installed
`sysadmin-v0.1.0`, `dyad craft list` shows `sysadmin 0.1.0 installed 9ff83da2 (modified)` — the
installed tree and the corrected tree differ on exactly this wording (diffed against the release's
`guards/changelog.py`). Cherry-picked on the fresh install there: applies cleanly, 85 tests pass,
no guard logic or FIELDS/ACTORS values moved — wording only.

Operator prompt behind the original fix (workstation #216, quoted by lan-git): "i agree with
keeping provenance for operator performed mutation, e.g. Gitea UI, and agent performed mutation,
e.g. CLI." — context, not a prompt for this row; not a provenance entry here (Rule-7 property 4).

## Mutation
`f2b9413` lives in `pltrinh1122/workstation`, not this repo — no remote configured, not fetched
(cherry-pick as the intake named it is not available). Implemented instead by deriving the same
wording change from the row's own stated semantic ("actor = hands, divergent credential in
outcome"), independently verified against this tree's own four occurrences of the "hands and
credential" phrase (a fifth site the intake didn't name, `crafts/sysadmin/templates/CHANGELOG.md`,
carries the same phrase and is included). Wording only — `ACTORS`, `FIELDS`, `OPTIONAL` and every
value stay unchanged; no guard logic moves, matching the intake's own claim that the real fix was
wording-only.

Craft zone (sysadmin), one PR:
- `crafts/sysadmin/rules/host-mutation.md`: `actor` redefined to name hands only; a divergent
  credential (the concrete case already true of every Agent-authored commit under Rule-2
  Enforcement — the Operator's own git identity, not the Agent's) is named in `outcome`.
- `crafts/sysadmin/guards/changelog.py`: the same reword in the module docstring and the
  `ACTORS` comment (comments only).
- `crafts/sysadmin/templates/CHANGELOG.md`: the same reword in the header note.
- `crafts/sysadmin/falsification/rules/host-mutation.md`: new attack row (91→92: the conjunction
  is undefined when hands and credential diverge, confirmed; survivor: hands only, credential in
  outcome), Disposition line gains `#92`.
- `crafts/sysadmin/VERSION`: 0.1.1 → 0.1.2 (0.1.1 already tagged for an unrelated bump, #100 PR5;
  tags do not move, Rule-11 property 4) — the drift guard (#91) requires it on any content change.

Infra zone, a second PR after the craft PR merges (Rule-1 referent before referrer; property 7
requires `BUNDLE.md`'s row to equal the live `VERSION` continuously, not just at release):
`BUNDLE.md` sysadmin row 0.1.1 → 0.1.2, alongside #103/#104's sysarch row (one infra PR covering
all three, since all three craft PRs land first). Bundle version 0.10.0 → 0.10.1.

Releases (separate Y/N each, Rule-11 Ratification, never batched): `sysadmin-v0.1.2`, and — with
#103/#104 — `sysarch-v0.2.1`, bundle `v0.10.1`.

## Files touched
`crafts/sysadmin/rules/host-mutation.md`, `crafts/sysadmin/guards/changelog.py`,
`crafts/sysadmin/templates/CHANGELOG.md`, `crafts/sysadmin/falsification/rules/host-mutation.md`,
`crafts/sysadmin/VERSION`; `BUNDLE.md` (second PR).

## Falsification
Intake shape: defect with evidence (path, commit, reproduction, mechanism verified) and an
Operator prompt behind the fix — admissible on either footing.

Attack: deriving the wording independently, rather than cherry-picking `f2b9413` itself, may not
match the sender's actual text. Result: survives, scoped — no test in this repo or the intake's
own report asserts literal prose (the intake's own evidence is "wording only, no guard logic or
FIELDS/ACTORS values moved"); the derived wording implements the same stated semantic ("actor =
hands, divergent credential in outcome") at every site of the phrase this tree actually carries,
one more site than the intake named. A byte-level mismatch with `f2b9413`, if any, is immaterial
to the claim it was fixing.
