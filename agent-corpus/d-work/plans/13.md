# Plan #13 — release v0.3.2, sysarch-v0.1.1, syseng-v0.1.2

Base commit of `main`: dc249a6. Refs #1 (the fix 0.3.2 ships), workstation#196 (workstation installs it).

## Intent as read
Publish the core craft at 0.3.2 (the #1 fix: suites and guards hold without sysadmin/lan-git) and the
two Tended crafts at their current VERSIONs (sysarch 0.1.1, syseng 0.1.2, bumped by #1 but untagged),
each as a git tag with its exported archive attached, so workstation can install them (Rule-11 p4, p5).

## Verified (read-only)
- `dyad check` green at dc249a6 (387 + 62 + 33 tests). `dyad build` -> dyad-0.3.1.tar.gz;
  `dyad craft export sysarch|syseng` -> sysarch-0.1.1.tar.gz, syseng-0.1.2.tar.gz (in scratch, not tracked).
- Tags: only v0.3.1. `.github/workflows/dyad-release.yml` already checks tag == v+VERSION, runs
  check, builds and attaches the core archive — but triggers on `v*` only; a craft tag
  (`sysarch-v0.1.1`, Rule-11 p4 prefix) would not run it and no craft archive would be published.
- Nothing else in the tree carries "0.3.1" as the version (README, manifest checked).

## Mutation
1. PR A, agent zone: `dyad/VERSION` 0.3.1 -> 0.3.2. One line.
2. PR B, infra zone: `dyad-release.yml` also triggers on `*-v*`; for a craft tag `<craft>-vX.Y.Z`
   it checks the tag equals `<craft>-v` + `crafts/<craft>/VERSION`, runs `dyad check`,
   `dyad craft export <craft>` and creates the GitHub Release with that archive; the core path is
   unchanged. Workflow is a library adapter (Rule-14): corroboration; the archives above were built
   on the kernel path.
3. After Done-Y merges A and B: three release counter-prompts, one per reply, Rule-11 form
   (`Y/N: release v0.3.2?`, then `sysarch-v0.1.1`, then `syseng-v0.1.2`) — Rule-11 binds a Y to one
   tag only, so "on the same Y" (the peer's wording) is not available. Each Y: create and push the
   tag (clerical); the workflow publishes the Release.
4. Message the workstation session when the three Releases exist.

## Files touched
dyad/VERSION; .github/workflows/dyad-release.yml; agent-corpus/d-work/{rows,plans,provenance}/13.md

## Falsification
- "The workflow already does it" — for the core, yes; for craft tags, refuted by the `v*` trigger.
- "Skip PR B, attach craft archives by hand" — refuted: Rule-11 p5, one code path tested in CI;
  a hand-attached archive is the Agent's inference. Survives: PR B.
- "Bump craft VERSIONs again" — refuted: #1 already bumped them and nothing changed since.
- Presence: no other session's file in `sessions/` (Rule-16); no overlap.
- Host actions: none. Destructive: none (tags are pushed, never moved or deleted).
