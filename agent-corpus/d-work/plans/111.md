# Plan #111 — rewrite the root README.md, then cut the deferred release

Base commit of `main`: 3f98732. Refs #109 (whose two tags, `dyad-operator-v0.8.3` and `v0.10.4`,
were held back pending `ds-syseng`), #110 (`ds-syseng`'s d-work, now `done`: a scoping-only plan,
no craft content changed — `main` green, versions unchanged at 0.8.3 / bundle 0.10.4).

## Intent as read
Sync (done: `origin/main` 8f012e0 -> 3f98732, four commits, all #110's ledger/record files),
then bring the root `README.md` up to date before the release, so the tag lands on a README that
describes the repository it is in.

What is stale, verified against the tree: the README says this repo is "package only: no
instance state (no ledger, no host corpus, no preferences values)" and "the distribution
channel" republishing an archive "built at release `v0.3.1` of the private development
repository" with a hardcoded sha256. None of that holds now — this repository holds the
development itself: `agent-corpus/` (ledger, provenance, incidents, falsification records),
`preferences-corpus/`, three authored Tended crafts under `crafts/` (sysadmin 0.1.2, sysarch
0.2.2, syseng 0.3.0), `BUNDLE.md`. It names "Rules 1–20" (17 and 21 are retired into the sysarch
craft; 19 Rule files exist: 1–16, 18–20). It never mentions the crafts, the bundle manifest, the
entrypoint, or the hooks. The hardcoded version and sha256 are exactly how it rotted.

## Mutation
Infra zone (`README.md` is a root file, `containment.py` zone `infra`), one PR, this file only —
outside every craft tree, so no `VERSION` moves and the drift guard is untouched. Proposed text,
in full (the plan-`Y` binds to it):

> # dyad-system
>
> The authoring repository of one Dyad Practice system: the core craft `dyad-operator` (`dyad/`)
> and the three Tended crafts it ships — `crafts/sysadmin/`, `crafts/sysarch/`, `crafts/syseng/`
> — developed here under their own Rules. The d-work ledger, provenance, incident log and
> falsification records live in `agent-corpus/`; the Operator's preferences in
> `preferences-corpus/`. The root `CLAUDE.md` holds one import line; the operating frame is
> `dyad/CLAUDE.md`.
>
> **Layout.** `dyad/` — the frame, the Agent Rules (`rules/`: 1–16 and 18–20; 17 and 21 are retired
> into the sysarch craft), the vocabulary, guards, templates, run-books and the falsification
> record behind every Rule; `dyad/README.md` maps it. `crafts/<craft>/` — one Tended craft each:
> rules, vocabulary, guards with tests, templates, records, installable on its own. `BUNDLE.md` —
> the bundle manifest, every craft in the tree at its live `VERSION`. `agent-corpus/` — instance
> state, never inside a craft.
>
> **Releases.** Every craft carries a `VERSION`; a release is a git tag —
> `dyad-operator-vX.Y.Z` for the core craft, `<craft>-vX.Y.Z` for a Tended craft, and the
> unprefixed `vX.Y.Z` for the bundle (`v` + the version `BUNDLE.md` names) — each cut only on the
> Operator's `Y` (Rule-11). Pin the whole distribution from one bundle Release, or only what you
> need from a craft's own tag. Tags do not move: a mistake is superseded by the next version,
> never retagged.
>
> **Install.** From a checkout, `dyad/bin/dyad install <path-to-repo>` writes `dyad/`, one import
> line in the host's `CLAUDE.md` and the documented, `dyad-`prefixed host-side hooks, never
> overwriting a host file; a second install changes nothing. `dyad/bin/dyad craft install
> <archive-or-tree>` writes `crafts/<craft>/` and its `crafts/REGISTRY.md` row only.
> `dyad/bin/dyad bundle build [dir]` builds every component `BUNDLE.md` names through the one
> distribution code path. Everything runs on the kernel alone — Python 3.12 and git
> (`dyad/infrastructure/INFRASTRUCTURE.md`).
>
> **Check.** `git config core.hooksPath dyad/hooks`, then `dyad/bin/dyad check` (the invariant
> pass, the tests, every guard of both roots); `check --guards` runs before every push, `check --pr
> <base> <head>` before a PR, and `check --evidence` prints the merge-evidence block (Rule-14).
>
> **Read** `dyad/CLAUDE.md` first, then `dyad/rules/` alongside `dyad/falsification/rules/`,
> `dyad/vocabulary/VOCABULARY.md`, `dyad/README.md`, and each craft's own `rules/`.
>
> Practice: [the-dyad-practice](https://github.com/The-Dyad-Practice-Commons/the-dyad-practice)
> and [dyad-bond](https://github.com/pltrinh1122/dyad-bond); not registered with the commons.

Then the release, unchanged from #109's held question: `dyad-operator-v0.8.3` and `v0.10.4`
together (Rule-11's release-batch form, #106 — both of one d-work's already-Done output, both
named here before the question), tagged on the post-README `main`.

## Files touched
`README.md`.

## Overlap (Rule-16)
Presence files: `dev-main`, `web-sysadmin-scope`, `web-sysarch-adh151`, `web-sysarch` (all stale)
and this session's own `unnamed-1bc8a044a4f6`; none declares `README.md`. `ds-syseng` keeps no
presence file (its own plan #110 says so); its work (`dyad/tests/`) is disjoint from this file.

## Falsification
Attack: a README that names versions, sha256s or a Release number is the most legible — the old
one did. Result: confirmed as the defect, not the fix — that text rotted at `v0.3.1` and stayed
wrong through fourteen releases. Survivor: the new text carries no version, sha or tag name;
versions are `BUNDLE.md`'s and each `VERSION`'s job, checked by `infra/bundle` on every push, and
the README only says where they live.

Attack: the "Install" claims (what a core install writes) can drift from `distribute.py` as
silently as the old version number did. Result: survives, scoped — every command named is one
`dyad/bin/dyad` prints in its own usage, and the install sentence restates Rule-11 property 2's
own wording rather than enumerating file names that `distribute.py` may change; the README says
"the documented, prefixed hooks" and points at the Rule, not at a file list.

Attack: this belongs in `dyad/README.md` (the package map), not the root. Result: refuted — that
file maps the package's own tree for someone already inside it; the root README is what the
hosting shows first and what the old text got wrong: which repository this is.
