# Plan #80 — falsify "dyad-system upgraded to 0.7.1"

Base commit of `main`: 9ed903a (re-planned; see Re-plan note). Refs #67 (the d-work that bumped the
version files), #68 (a peer session's finding on the same three d-works), #69 and #70 (a peer
session's own falsification of the same claim, and its own open question this plan resolves).
Falsification: inline below.

## Re-plan note (Rule-15 phase 2)
First written as id 69. Before landing, `main` had moved past `agent-corpus/d-work/rows/69.md`:
`dev-main` opened its own `#69`, "falsify 'dyad-system upgraded to 0.7.0'" — the same exercise,
independently — and its own `#70` (backlog): "no distinct `Y/N: release <tag>?` disposition is
recorded for sysadmin-v0.1.0, v0.7.0, dyad-operator-v0.6.1, sysarch-v0.1.5 or v0.7.1... confirm
against the Operator's own chat record before deciding whether this is a gap in the record or in
conduct." Renumbered to 71, then a fifth collision (dev-main took 71 too, "syseng drifted...bundle 0.7.2") before this landed either -- renumbered again, to 80, real headroom above the observed max rather than another +1 chase.

That overlap changes the mutation, not just the id. `#70` already states the finding this plan's own
falsification reached (no release-Y recorded for any of the five tags); duplicating it as a second,
separate `INCIDENTS.md` row would be noise on the same three d-works `#68` already occupies once.
What this plan still contributes, and #70 does not yet resolve, is *which* of its two named
possibilities is true: `#70` asks whether the missing disposition is a gap in the *record* (asked
and given, just not written down) or in *conduct* (never asked). This plan's own evidence answers
that directly — `dyad-release.yml` triggers only on a pushed tag or a dispatch naming one that
already exists, so nothing could have manufactured a release without the `Y` first; and `#67`'s own
plan named the three release counter-prompts as a scheduled follow-up, so if they had been asked
they would appear in that plan's own provenance record, and they do not. Conduct, not record.

That answer belongs on the record as this session's own delivered finding — not as an edit to
`#70`, which is `dev-main`'s own backlog row and not this session's to write into (Rule-16: a
session's own claims stay in its own files). The mutation below is sized to that: no independent
`INCIDENTS.md` row, since #70 already carries the open question and this plan's evidence resolving
it is stated here, in this session's own provenance and plan.

## Intent as read
"dyad-system upgraded to 0.7.1. falsify." — check the claim by Rule-9's own method, not by reading
`BUNDLE.md`'s one line and accepting it.

## Finding
Six attacks (reproduced in the reply that opened this d-work), all failed to sustain the claim as a
release. Verified directly: `git tag -l` has no `v0.7.0`, `v0.7.1`, `dyad-operator-v0.6.1`,
`sysarch-v0.1.5`, or any `sysadmin-v*` at all; the bundle's own tag history stops at `v0.5.1`,
`dyad-operator` at `v0.5.1`, `sysarch` at `v0.1.2`. `dyad-release.yml` triggers only on a pushed tag
or a dispatch naming an existing one — nothing in the pipeline can substitute for the `Y/N: release
<tag>?` Rule-11 requires. Row `#67`'s own plan named three release counter-prompts "asked separately
after Done-`Y`"; `#67` is `done` and none of the three appears in its `disposed` column or its
provenance. The step the plan named was never executed — not skipped by decision, just never asked.

Survivor: "dyad-system upgraded to 0.7.1" is false as a release claim. What's true is narrower — the
bundle manifest and four `VERSION` files were edited to a mutually consistent 0.7.1/0.6.1/0.1.5/
0.1.3/0.1.0, and the bundle guard (a bookkeeping check only) reports that consistency as `ok`. No
tag, archive, or GitHub Release exists at any of those numbers for any of the four components.

## Mutation
None beyond the ledger. This d-work's output is the falsification itself — delivered in the reply
that opened it, restated above, and recorded in this plan and in `provenance/80.md`. No PR, no
corpus file outside `agent-corpus/d-work/`.

**Not in this mutation, stated:**
- No `INCIDENTS.md` row: `#70` (`dev-main`'s) already holds the open question this plan resolves;
  a second row restating the same verdict on the same three d-works is duplication, not signal.
- No tag is cut here. Whether to release `v0.7.1` (and the two craft tags under it) is the
  Operator's disposition, not this d-work's.
- `#68`'s finding (self-ratified merges on `#47`, `#58`, `#67`) is not restated here; different
  fault, already being recorded by the session that found it.
- Nothing is written to `#69` or `#70` — those are `dev-main`'s own rows.

## Files touched
agent-corpus/d-work/rows/80.md; agent-corpus/d-work/plans/80.md; agent-corpus/d-work/provenance/80.md

## Falsification
Reproduced from the reply that opened this d-work; six attacks, results as given there:
1. The `BUNDLE.md` line and a green guard are enough. Refuted as sufficient — the guard checks
   internal consistency, never that a release happened; the vocabulary defines release as a tag.
2. The bundle was tagged at 0.7.1, or 0.7.0 before it. Refuted — no such tags exist; the bundle's
   tag history stops at `v0.5.1`.
3. Its components were tagged individually even if the bundle wasn't. Refuted — `dyad-operator` and
   `sysarch` tags are both behind their `VERSION` files by multiple bumps.
4. `sysadmin` shipped as part of this bundle release. Refuted — zero `sysadmin-v*` tags exist ever.
5. The release workflow will pick this up automatically. Refuted — it triggers only on a pushed tag
   or a dispatch naming an existing one; nothing automates the Operator's own ratification.
6. This is an isolated, fresh defect. Refuted, and compounded — `#67`'s own plan scheduled the
   release counter-prompts and they never fired; `#68` (a peer session) independently found the
   merges landing this same version bump were themselves under-ratified. Two distinct faults on the
   same three d-works, not one.
- **Overlap (Rule-16):** presence read before writing this plan. `dev-main` (stale) claims rows
  including `#56/#57`, none of which this plan touches. `web-sysarch` (live, same root) claims
  `#56 #58`, `files=0` — no file declared. This plan (as re-planned) writes only its own ledger
  files under `agent-corpus/d-work/`; no overlap found.

## Amendment — this d-work, before Done-Y (Rule-3 Incidents)
The Finding and Survivor above are **wrong**, and merged into the corpus that way (PR #55). While
gathering completion evidence, `agent-corpus/falsification/dyad-system-0-7-release-claim.md` landed
(`dev-main`'s own record on the same claim) naming real tags: `v0.7.0` (2026-09-17 17:10:06),
`v0.7.1` (2026-09-17 17:41:55). A direct check (`git fetch --tags origin`, never previously run this
session) confirms them, plus `dyad-operator-v0.6.0`, `dyad-operator-v0.6.1`, `sysarch-v0.1.5`,
`sysadmin-v0.1.0` — every tag attacks 2–4 above said did not exist.

**Root cause, this session's own:** every check this plan's Finding relies on used
`git fetch origin main` — an explicit refspec, which suppresses git's normal tag-auto-following.
That command never fetched a single tag, all session, however many times it ran. "No tag exists"
was concluded from a local view that had never looked, not from reality. The tags were already
live on the remote, cut the day before this d-work opened.

**Corrected Survivor:** "dyad-system upgraded to 0.7.1" is **true** as a release claim — attacks
2–4 are themselves refuted, by the tags this session failed to fetch. What survives, independently
confirmed by `dev-main`'s own separate record on the same claim: neither `provenance/58.md` nor
`provenance/67.md` holds a third, distinctly-worded `Y/N: release <tag>?` disposition — only the
ordinary plan/done pair each. So the releases exist without a corpus-recorded release-ratification.
This plan's earlier framing of that gap as settled "conduct, not record" is retracted as
overconfident: `dev-main`'s own attack 5 states it correctly — cannot be determined from the corpus
alone; left for the Operator to confirm against the Operator's own copy of the chat.

## Mutation (amended)
One PR, **agent zone**, citing `d-work #80`:
1. `agent-corpus/audits/INCIDENTS.md` — one row: this session's own tag-fetch gap
   (`git fetch <remote> <branch>` never follows tags; a tag-existence check needs `git fetch --tags`
   or a bare `git fetch <remote>`), and its consequence — a wrong, six-attack falsification
   conclusion, merged before being caught.

The rest of the plan (Finding, original Survivor, original Falsification table) is left as written
above rather than rewritten, so the record shows the actual mistake rather than a cleaned-up trail;
this Amendment is the correction of record.

## Files touched (amended)
agent-corpus/audits/INCIDENTS.md; agent-corpus/d-work/rows/80.md; agent-corpus/d-work/plans/80.md;
agent-corpus/d-work/provenance/80.md
