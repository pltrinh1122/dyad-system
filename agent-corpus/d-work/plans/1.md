# Plan #1 — remediate the red Actions runs on dyad-system

Base commit of `main`: 2dad9be. The first d-work of this instance. Refs workstation#194 (the seed),
workstation#184 (core tests hardcode sibling crafts), Rule-11 p5, Rule-12.

## Intent as read
`https://github.com/pltrinh1122/dyad-system/actions` shows failed runs; make the workflows green
and keep them green.

## Verification (read-only, 2026-09-15; every failing job's log read)
Actions run here — a public repo has free minutes, so the private repo's billing block does not
apply. Eleven runs, five red, in three classes:
1. **Bootstrap detections, one-time** — `rule-3 d-work` (main fence: the three seed commits are
   direct pushes to `main` touching non-ledger paths) and `rule-1 containment` (range mode: the push
   `f6bd4fa..2dad9be` spans agent, preferences and craft zones). Both detectors did their job on
   pushes the Operator ratified as direct pushes (workstation#194 Done-Y, Rule-2). Each future push
   is judged on its own range, so these do not recur. No history rewrite; the runs stay as the record.
2. **`rule-11 release` on `v0.3.1`** — `agent/frame`: `preferences-corpus/PREFERENCES.md` did not
   exist at the tag (package-only repo). It exists since the seed; the next tag will not hit it. Its
   Rule-12 failures are class 3.
3. **`rule-11 package`, both runs — the real defect:** `package.py check` is red in a system that has
   the core, `sysarch` and `syseng` but not `sysadmin` or `lan-git`. Every red item assumes those:
   - `dyad/tests` (15): `guards/agent/test_references.py` (11) resolves the `changelog`/`event`/`ops`
     kinds through the *live* sysadmin guards, absent here, and asserts "every kind ran";
     `test_dyadlib.test_this_instance_still_refuses_its_host` (workstation#192) assumes this instance
     has a `package_rules.local.txt`; `test_package.test_list_prints_registry` and
     `test_runbook_subcommand_dispatches` derive expectations from a `CRAFTS` flag that says "crafts
     present" without asking *which*; `test_scratch_install_with_one_row_survives_referential_integrity`
     installs from a source tree that lacks the guards its own check expects.
   - `crafts/sysarch/tests` (2): `test_project_entities` keys the live registry by `ops_scripts`
     (`KeyError`); `test_project_kanban.LiveTests` asserts more than 100 rows (workstation#186 —
     this instance has one).
   - `crafts/syseng` (2 guard FAILs + 2 tests): `naming_rules.txt` lists `env: DYAD_OPS`, read only
     by sysadmin's `ops_scripts.py` — "stale" here by the symmetric check (workstation#185);
     `invariants_rules.txt` exempts `crafts/*/server/*.py`, lan-git's — "matches no module".

## Falsification (Rule-9)
| # | attack | result | survivor |
|---|---|---|---|
| 1 | Install sysadmin and lan-git here too; the suites were written for that world. | Refuted | They carry host and server content and are authored in workstation by the Operator's split (#194). A core craft whose own checks need a host craft installed is not distributable (Rule-11 p5: install into a scratch repo, run the guards). The suites are wrong, not the system. |
| 2 | Skip everything sibling-dependent with `skipUnless`. | Survives, scoped | Right for tests *about* a sibling's parsers (the sysadmin-only reference kinds, the run-book template): skip with the reason printed, keyed on `dyadlib.find_guard`. Wrong for counts and registries: those derive the expected set from what is installed, so they still assert something here. |
| 3 | The syseng data rows are sysadmin's and lan-git's; the fix is craft-contributed data files (`crafts/<craft>/guards/naming_rules.txt`) the syseng guards merge — the pattern `references.md` already names. | Survives, deferred | The cleanest end state, but it changes two guards' data model and needs matching files authored in workstation at the same moment. Smaller now: an `@<craft>` qualifier on an `env:` token (`DYAD_OPS@sysadmin`: checked only when that craft is installed) and an exempt glob under an absent `crafts/<c>/` treated as vacuous. One table, two small code paths, tests. Craft-contributed files: a follow-on named here. |
| 4 | This leaves workstation's copies red until it installs the new versions. | Confirmed | Sequence: land here, release core v0.3.2, sysarch 0.1.1, syseng 0.1.2 (Rule-11 tags), then workstation installs all three — its own d-work, which closes workstation#184. Until then workstation runs the old suites, which pass there. |

## Mutation
1. **Agent-zone PR:** `dyad/tests/guards/agent/test_references.py` — expected resolvable set = kinds whose parser `find_guard` provides; sysadmin-kind tests `skipUnless`; `test_dyadlib`: the host-refuse test skips when no local file; `test_package`: `CRAFTS` becomes "which crafts" (registry expectation from `dyadlib.craft_dirs`, run-book test keyed on a craft providing `templates/runbook.md`), scratch-install test installs a source that includes the installed crafts' guards or asserts only the core kinds. Incident row in `audits/INCIDENTS.md` for the five red runs.
2. **Craft-zone PR:** `crafts/sysarch/tests/test_project_entities.py` builds its guard map from the live registry; `test_project_kanban` live test asserts count == store size; `crafts/syseng/guards/naming.py` + `naming_rules.txt`: `env: TOKEN@craft`; `invariants.py`: exempt under an absent craft is vacuous; `VERSION` bumps sysarch 0.1.1, syseng 0.1.2; tests.
3. Evidence: `check --evidence` green on each head; then `rule-11 package` green on `main` after merge — the first observed green run here.
4. Release: `dyad/VERSION` 0.3.2 (own PR); on the Operator's `Y/N: release v0.3.2?` the tag; `rule-11 release` should now build and attach the archive itself. Craft tags `sysarch-v0.1.1`, `syseng-v0.1.2` on the same Y (Rule-11 p4). Installing them into workstation: workstation's d-work, next.

## Files touched
dyad/tests/guards/agent/test_references.py, dyad/tests/test_dyadlib.py, dyad/tests/test_package.py,
agent-corpus/audits/INCIDENTS.md, crafts/sysarch/tests/test_project_entities.py,
crafts/sysarch/tests/test_project_kanban.py, crafts/sysarch/VERSION, crafts/syseng/guards/naming.py,
crafts/syseng/guards/naming_rules.txt, crafts/syseng/guards/invariants.py, crafts/syseng/guards/invariants_rules.txt,
crafts/syseng/tests/guards/test_naming.py, crafts/syseng/tests/guards/test_invariants.py, crafts/syseng/VERSION, dyad/VERSION.

## Counter
Delete the five red runs (`gh run delete`) and change nothing: the bootstrap reds are one-time and
the package reds only bite the public repo's dashboard. Reconciliation: the package reds are a real
defect in the shipped suites — the core craft's own check fails on a clean install, which Rule-11 p5
promises never happens — and every future push and release here would stay red.

Y/N: proceed with #1 as planned?
