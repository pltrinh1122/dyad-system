# Plan #36 — kanban surface of every d-work

Base commit of `main`: 7d242a0. Refs #22 (its `--craft` filter), sysarch d-work #186 (the projector).

## Intent as read
A surface showing every d-work of this repo as a kanban board, one column per state.

## Finding (Rule-13: reuse before authoring)
It exists: `crafts/sysarch/projectors/project_kanban.py` (sysarch craft, d-work #186; `--craft`
filter by the web session's #22). `dyad project kanban` projects `dyadlib.read_rows` onto one column
per `dyadlib.STATES` (open, planned, blocked, backlog, done), one card per row (id, title, refs,
disposition count), blocked cards showing their blocker, `done` collapsed behind `<details>`;
deterministic, self-contained HTML at `<instance>/projections/kanban.html` — a generated path
(`package_rules.txt`), never tracked. Run: 34 rows → open 2, planned 4, blocked 0, backlog 13,
done 15. Delivered to the Operator as a rendered file.

## Mutation
None. Nothing to build; the run writes only a generated, ignored file. If the surface needs
something it lacks (a title filter, a column for the web session's rows vs this one's, refs as
links), that is a sysarch craft change — a new prompt, craft zone.

## Files touched
agent-corpus/d-work/{rows,plans,provenance}/36.md

## Falsification
- "The prompt says build, so build" — refuted: Rule-13 (reuse over inference; import over author)
  and the projector already meets the prompt's every clause. Building a second one would breach
  `projection.md` p4 (one craft per surface) as well.
- Presence: `web-sysarch` on #22–#33; no overlap. Host actions: none.
