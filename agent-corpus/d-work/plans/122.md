# Plan #122 — incident mode F: form error in a clerical record (4 incidents: row born planned, d-work #N in prose, unstaged file)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode F: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode F)
#1 (row born planned, doubled date); #16 (row born planned); #15 (d-work #141 in prose read as a claim by prs.py); #17 (record unstaged when the scratch-install suite ran). Secondary in #67: ';' in disposed text, literal 'Y plan' needed

## Falsification (candidate remediation)
Two of four are one defect: dwork new followed by dwork state planned in one commit. Candidate survivor: dwork state refuses a transition on an uncommitted new row; dwork state -d rejects a ';' in the text (the cache-only recurrence, five occurrences). Attack: the guard rows.py already fails these on main -- yes, after the push; the candidate moves the refusal to the command.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/122-mode-F`): `agent-corpus/audits/2026-09-22-incident-mode-F.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-F.md`

## Amendment — 2026-09-22 (naming correction)
The record's file name is `agent-corpus/audits/2026-09-22-incident-mode-f.md`, lowercase:
`syseng/naming` requires the slug `[a-z0-9-]+` under `<instance>/audits/`, which the uppercase
mode letter this plan first named fails. Caught by the combined `check --evidence` run over all
ten branches before any merge; corrected in a second commit on this d-work's branch.
