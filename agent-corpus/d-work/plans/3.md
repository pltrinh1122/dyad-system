# Plan #3 — why haven't you pushed?

Base commit of `main`: 2dad9be as read; eb9775c when executed.

## Intent as read
The Operator expects the ledger commits on `origin/main`. The pre-push hook failed on stale
`syseng` guard data (`invariants_rules.txt` exempt line, `naming_rules.txt` env DYAD_OPS) and on the
main fence (row #1 added in state `planned`).

## Mutation (as authorized in chat)
Rewrite the local ledger commits open→planned→done; craft-zone branch removing the stale lines;
one `--no-verify` push of that branch; PR; merge on Done-Y.

## Outcome — superseded
Before execution, `origin/main` had moved: a concurrent session's d-work #1 (PRs #1, #2) removed the
same stale data and made the suites pass; `dyad check --guards` is 0 FAIL at eb9775c. The craft-zone
change and the `--no-verify` push are withdrawn (departure, Rule-3). Only the ledger rewrite remains,
done as rows #2 and #3.

## Files touched
agent-corpus/d-work/rows/3.md, agent-corpus/d-work/provenance/3.md, agent-corpus/d-work/plans/3.md
