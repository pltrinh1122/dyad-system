# Plan #16 — release workflow: full history and dispatch on an existing tag

Base commit of `main`: 0b4943e. Refs #13 (its v0.3.2 tag run failed: run 34926465612).

## Intent as read
Publish the Release for the already-pushed `v0.3.2` tag (and, after their own Ys, the craft tags)
through the one tested code path, without moving a tag.

## Verified (read-only)
Shallow clone of v0.3.2: `package.py check` 6 FAIL (agent/references plan.base->commit x4; two
history-dependent tests). After `git fetch --unshallow`: 0 FAIL. The other three workflows set
`fetch-depth: 0`; dyad-release.yml did not. A tag-push run uses the workflow at the tag, so a fix on
`main` needs a dispatch path to reach an existing tag.

## Mutation
One infra-zone PR: `.github/workflows/dyad-release.yml` — `on.workflow_dispatch.inputs.tag`;
checkout `ref: ${{ inputs.tag || github.ref }}` with `fetch-depth: 0`; the tag name read from
`inputs.tag` when dispatched, else `GITHUB_REF_NAME`; steps otherwise unchanged. Then
`gh workflow run dyad-release.yml -f tag=v0.3.2` (clerical: the release Y of #13 covers this tag).

## Files touched
.github/workflows/dyad-release.yml; agent-corpus/d-work/{rows,plans,provenance}/16.md

## Falsification
- "Re-tag after the fix" — refuted: tags never move (Rule-11 p4 conduct; workflow never edits one).
- "Release v0.3.3" — survives but wasteful: identical content, orphan tag. Not chosen.
- "Hand-upload the deterministic archive" — refuted by Rule-11 p5 (one code path, in CI). Fallback only.
- Dispatch on a tag whose VERSION mismatches still fails the tag check: the guard holds.
- Host actions: none. Presence: no other session file.
