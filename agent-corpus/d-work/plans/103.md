# Plan #103 — workstation-252: sysarch test_project_entities.py:205 hardcodes a second craft-corpus map

Base commit of `main`: 3f3ca32. Refs workstation-252 (via `dsv0.5.1: workstation`, 2026-09-20,
message 3 report 2), #98 (same bug class, fixed at a different location in the same file), #46
(the original occurrence, `dyad/tests/test_package.py`).

Prompted 2026-09-20 (Operator `Y` to the batched proposal covering #92, #103, #104): fix now.

## Intent as read (the sender's observation, verbatim as carried)
`crafts/sysarch/tests/test_project_entities.py:205`:
```
else: self.assertEqual(e.corpus, {"sysadmin": "workstation", "sysarch": "craft", "syseng": "craft", "lan-git": "craft"}[e.guard.split("/")[1]])
```
`e.corpus` is read straight off each guard module's own `CORPUS` attribute by `project_entities.
collect()` (verified: `crafts/sysadmin/guards/changelog.py` `CORPUS = "workstation"`,
`crafts/sysarch/guards/registry.py` `CORPUS = "craft"`, etc.). On workstation's tree, `crafts/
automaton/` is installed with a guard whose craft name ("automaton") is absent from the literal
dict, and the subscript raises `KeyError: 'automaton'` — the same #46/#98 shape (a hand-enumerated
craft-name map that a new craft silently falls outside of), at a *different* line than the one
#98 fixed (that fix rewrote `ENTITIES` at line ~199; this map at line 205 is untouched by it — my
own PR3 work did not sweep the file for a second instance of the same pattern).

## Mutation
Craft zone (sysarch), one PR (with #104):
`crafts/sysarch/tests/test_project_entities.py`: add a module-level `KNOWN_CRAFT_CORPUS` dict
(the crafts this repo ships, documented so a regression here still fails) mirroring
`dyad/tests/test_package.py`'s `KNOWN_CRAFTS`/`craft_guards()` fallback pattern (the #46 fix,
`../../../dyad/tests/test_package.py` lines 7-15): for a craft in the dict, assert `e.corpus`
equals the documented value (unchanged strictness for `sysadmin`, `sysarch`, `syseng`,
`lan-git`); for a craft not in the dict — the case a peer's own craft like `automaton` hits —
assert only that `e.corpus` is one of `containment.ZONE_NAMES` (already loaded in this file as
`containment`, line 13), never a `KeyError`. `crafts/sysarch/VERSION`: 0.2.0 → 0.2.1.

Infra zone, a second PR after the craft PR merges (with #92, #104): `BUNDLE.md` sysarch row
0.2.0 → 0.2.1, bundle version 0.10.0 → 0.10.1.

Release (separate Y/N, Rule-11 Ratification, never batched): `sysarch-v0.2.1` (with #104), and —
with #92 — bundle `v0.10.1`.

## Files touched
`crafts/sysarch/tests/test_project_entities.py`, `crafts/sysarch/VERSION`; `BUNDLE.md` (second PR).

## Falsification
Intake shape: defect with evidence (path, line, exact content matching the report, reproduction —
`KeyError: 'automaton'` — mechanism verified: `e.corpus` traced to each guard's own `CORPUS`) —
admissible.

Attack: a membership check against `ZONE_NAMES` for an unknown craft is weaker than the exact
per-craft assertion — it would not catch a craft that declares the *wrong* zone (e.g. a craft
whose guard wrongly sets `CORPUS = "infra"`). Result: survives, scoped — the #46 precedent accepts
the same weakening for the identical reason (no test in this repo can know a craft's correct
corpus without either hardcoding it, which is the bug, or reading it from the craft's own
documentation, which does not exist as parseable data); the exact check still applies to every
craft this repo actually ships, so a regression among *known* crafts still fails.