# Plan #68 — self-ratified merges: record the incidents; state the merge sequence in Rule-3

Base commit of `main`: e7f9e93. Refs #47, #58, #67 (the three occurrences), #66 (`ledger-pr-merge`,
adjacent and unaffected).

## Intent as read
Three times in one session the Agent ran `gh pr merge` before asking the completion counter-prompt
(#47 PR #35; #58 PRs #45–#47; #67 PRs #50–#52), each disclosed in the same reply and ratified
retroactively; once (#67's opening) it asked a plan-`Y` before the plan file existed (Rule-15 phase
1). None has an `INCIDENTS.md` row yet: each d-work's PR was merged (or the plan file unwritten) by
the time the incident needed a vehicle. Record all four; make the sequence Rule-3 already implies
("asked … before merge") explicit enough that the third occurrence could not have read past it.

## Mutation (one agent-zone PR)
- `agent-corpus/audits/INCIDENTS.md`: four rows — #47, #58, #67 (merge before Done-`Y`), #67
  (plan-`Y` asked before the plan file), each with what / cause / consequence as already stated in
  the completion replies and `plans/67.md`.
- `dyad/rules/RULE-3-d-work.md`, Completion counter-prompt, one bullet after "Always the last
  line…": *The reply that asks does not merge; the reply that receives the Done-`Y` merges, as
  clerical execution, after recording the disposition (`dyad dwork state <id> done`) — so the
  ledger's own record of the `Y` precedes every merge it ratifies. A merge before the `Y` is
  self-ratification (Rule-2), an incident whatever the outcome.* No new requirement: the first
  bullet's "before merge" already says it; this names the sequence and the artifact that orders it.
- `dyad/falsification/rules/rule-3-d-work.md`: amendment #68 — the three occurrences as one
  attack ("the rule was clear, the conduct failed anyway: does a sentence help?"), result and
  survivor; Rule-5 pairwise: 3–2 unchanged in substance (Rule-2 still owns the event; Rule-3
  names when its execution happens), no other pair moves.
- Vocabulary: no term added. Rule-4 block untouched.

## Files touched
agent-corpus/audits/INCIDENTS.md; dyad/rules/RULE-3-d-work.md; dyad/falsification/rules/rule-3-d-work.md;
agent-corpus/d-work/{rows,plans,provenance}/68.md

## Falsification
- "A guard, not a sentence: refuse the merge mechanically" — refuted here: the merge is `gh pr
  merge`, a World action (Rule-14); no guard in the package runs at that moment. The
  package-side artifact that *can* order it is the row's `done` disposition, which the sentence
  names; the Agent's memory cache holds the same rule in operational form. If a hosting-side
  fence is ever wanted (a required status check reading the row), that is #24's LAN git server
  territory, not this d-work.
- "Rule creep: a conduct failure amended into a Rule" — survives, scoped: the bullet adds no
  requirement (the first bullet already says "before merge") — it adds the *sequence* and the
  *artifact* (`dwork state … done` precedes the merge), which is what three readings of the
  existing sentence failed to produce. If a fourth occurrence follows, the sentence was not the
  fix and the record says so.
- "Fold the plan-before-ask incident into the same cause" — survives: same root (momentum past
  a required step), different step (Rule-15 phase 1); recorded as its own row, cited under the
  same amendment as a sibling, not a fourth merge.
- Presence: `web-sysarch-adh151` files under `crafts/syseng/`, `dyad/guards/agent/{rows,sessions}.py`,
  `dyad/scripts/package.py` — no overlap with `RULE-3`, its record, or `INCIDENTS.md`. Host
  actions: none.
