# Plan #19 — reset repo secrets, hand-publish v0.3.2, park CI-native releases

Base commit of `main`: 49e9988 (main; PR #8 not yet merged). Refs #18 (superseded release-auth
attempt), #13/#16 (the release this delivers).

## Intent as read
Superseded twice in one thread (E1, E2 below) down to what the Operator actually needs: `v0.3.2`
published, without building new infrastructure to get there. Reset the repo's secrets to a clean
slate; publish the release the way already proven to work; leave "make CI itself able to publish a
release" as an unstarted backlog item, not this d-work's problem.

## Verified (read-only)
Two secrets exist: `RELEASE_TOKEN` (mine, this session) and `RELEASE_SECRET` (unexplained origin,
unreferenced anywhere in the tracked tree). The claude.ai GitHub App already connected to this
session (its MCP tools) has no release-creation capability — only `get_latest_release`,
`list_releases`, `get_release_by_tag` — so it could not have replaced building `DS_CLAUDE` either.
What *is* already proven, twice, this session: `gh release create` (and a raw REST POST) succeed
immediately using ordinary `gh` auth run from here — outside Actions, where the 403 lives.

## Mutation
**Phase 1 (now, on `Y`):**
1. Delete `RELEASE_TOKEN` and `RELEASE_SECRET` from the repo's Actions secrets. Recommend, stated
   not executed (I hold no credential to revoke): the Operator also revoke the two PATs themselves
   at github.com/settings/tokens — deleting the Actions secret removes the *usage*, not the
   credential's own validity elsewhere.
2. Close PR #8 (superseded, not merged) and d-work #18 (superseded).
3. Publish `v0.3.2` by hand: `gh release create v0.3.2 dyad-0.3.2.tar.gz --title v0.3.2 --notes
   "dyad package v0.3.2"` against the already-pushed tag, using the local, deterministic archive
   (`dyad build`, sha256 checkable against anyone else's build of the same tree). Clerical execution
   of the release `Y` already given under #13 — no new ratification event.
4. Open one `backlog` row: "CI publishes a Release automatically on a tag push — currently 403s
   from the Actions runner for every credential tried (automatic token, fine-grained PAT); a
   GitHub App's installation token was the next untried idea, not pursued (Operator, #19)." Not
   started; the workflow's `check`/`build` steps stay as they are (still real, still useful) and
   its Release-creation step is left in place, unreachable until that backlog item is picked up.
5. If the Operator already clicked through to create `DS_CLAUDE` before this reply: no cleanup
   forced — an unused App sitting idle costs nothing; deleting it is the Operator's call, not
   planned here.

## Files touched
agent-corpus/d-work/{rows,plans,provenance}/19.md; a new backlog row.

## Falsification
- E1 (first correction): Phase 2's original App-creation walkthrough omitted required fields
  (Homepage URL) and the Webhook-Active trap. Confirmed live by the Operator against the actual
  form. Caught before any `Y`.
- E2 (this correction): the Operator asked whether any of Phase 2/3 was necessary at all, given a
  GitHub App already exists for this session. Checked directly (its tool list) rather than assumed:
  it can't create releases either way, and the real unblock (hand-publish) was already sitting
  proven and unused since the first AskUserQuestion. Building `DS_CLAUDE` into CI is deferred to a
  named backlog row, not abandoned silently.
- "Delete the Release-creation step from the workflow entirely, since it's unreachable" — refused:
  it costs nothing idle, and removing it would need re-adding when the backlog item is picked up;
  simpler to leave it as dead code with a known cause than to edit `main`'s workflow twice.
- Host actions: none. Presence: no other session file.
