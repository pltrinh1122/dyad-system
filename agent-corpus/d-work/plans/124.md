# Plan #124 — incident mode H: fence collision not surfaced (1 incident: ledger-only PRs parked without asking, #65)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode H: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode H)
#65 (five ledger-only PRs opened and parked; the 'state the rest and ask' half of Rule-1's #23 case omitted; named by the Operator)

## Falsification (candidate remediation)
Closed by #66: ledger-pr-merge = agent now says who merges a fenced ledger-only PR. This child records the count and verifies the preference row is live; no further mutation. Attack: one incident is not a mode -- it is the only incident named by the Operator rather than detected by the Agent, which is why it is kept separate.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/124-mode-H`): `agent-corpus/audits/2026-09-22-incident-mode-H.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-H.md`

## Amendment — 2026-09-22 (naming correction)
The record's file name is `agent-corpus/audits/2026-09-22-incident-mode-h.md`, lowercase:
`syseng/naming` requires the slug `[a-z0-9-]+` under `<instance>/audits/`, which the uppercase
mode letter this plan first named fails. Caught by the combined `check --evidence` run over all
ten branches before any merge; corrected in a second commit on this d-work's branch.
