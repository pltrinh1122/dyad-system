# Plan #67 — bump dyad-operator and sysarch for their already-landed drift; re-release

Base commit of `main`: 568b217. Refs #58 (the drift's largest single contributor), #50, #45, #47.

## Intent as read
Workstation's reply to the v0.7.0 message (Operator prompt, relayed): a fix in `dyad/tests/`
"cannot reach us through a reinstall unless those archives differ from v0.6.0's under unchanged
version numbers" — correctly inferring that they don't differ, because `VERSION` never moved while
tracked content did. Verified directly (not taken on the peer's word alone): `git diff --stat
dyad-operator-v0.6.0..main -- dyad/` shows 9 files changed from #47 and #58, `dyad/VERSION` static
at 0.6.0 across both. `git diff --stat sysarch-v0.1.4..main -- crafts/sysarch/` shows 5 files
changed from #45, #50 and #58, `crafts/sysarch/VERSION` static at 0.1.4 across all three. Every
"0.6.0" and "0.1.4" claim since those tags cut has been false.

## Mutation
- **Agent zone:** `dyad/VERSION` 0.6.0 → 0.6.1 (patch: test and doc fixes only since the tag, no
  behavior change to a Rule or a guard's semantics — `references.py`'s filter is the one functional
  change, #47, already released once as part of `dyad-operator-v0.6.0`... no: #47 merged *after*
  that tag. Patch is still right: it's a bug fix, not new surface).
- **Craft zone:** `crafts/sysarch/VERSION` 0.1.4 → 0.1.5 (patch: same reasoning — `project_instances.py`,
  #50, is new surface and would argue minor, but it shipped silently under 0.1.4 already in
  practice on `main`; a patch bump now stops the bleeding without relitigating what #50 should have
  done).
- **Infra zone:** `BUNDLE.md` rows `dyad-operator | 0.6.1`, `sysarch | 0.1.5`; `version: 0.7.0 →
  0.7.1`.
- Three PRs, one zone each, merged in dependency order under one Done-`Y` (the #21/#6/#37/#58
  pattern): agent and craft first (either order, disjoint), infra last (needs both VERSIONs moved).
- Releases, asked separately after Done-`Y`: `dyad-operator-v0.6.1`, `sysarch-v0.1.5`, `v0.7.1`.
- **Not in this d-work:** a mechanical guard against this recurring (no released craft's tracked
  content should differ from its last release tag while `VERSION` doesn't) — proposed as its own
  d-work, named in the completion reply, not designed here.

## Files touched
dyad/VERSION; crafts/sysarch/VERSION; BUNDLE.md; agent-corpus/d-work/{rows,plans,provenance}/67.md

## Falsification
- "Bump to the next minor (0.7.0 / 0.2.0), not patch" — considered, refuted for now: nothing in the
  accumulated diff is new *contract* surface from this fix's own vantage (project_instances.py is
  already live and already shipped once under 0.1.4; re-numbering it as "new" a second time, after
  the fact, invents a boundary the actual git history doesn't have). Patch says "same guarantees,
  content corrected" — the honest claim. If the guard proposed in Mutation later disagrees, that's
  its own falsification.
- "Fix it by re-tagging `dyad-operator-v0.6.0` and `sysarch-v0.1.4` to their current commits
  instead" — refuted: Rule-11's own provenance line states it plainly for an earlier case ("tags do
  not move"); a moved tag breaks anyone who already fetched the old one, silently.
- "This is really a new d-work per drifted craft, not one" — refuted: both fixes are the identical
  mechanical action (bump, row, release) discovered together, from one finding; splitting doubles
  the ceremony for no independent risk.
- Presence: `web-sysarch`/`web-sysarch-adh151` at `/home/user/dyad-system` — a different host;
  no file overlap (`dyad/VERSION`, `crafts/sysarch/VERSION`, `BUNDLE.md` claimed by neither's
  `files:`). Host actions: none.

## Incident (this d-work's own opening)
Asked this d-work's Y/N before opening the row or writing this plan file — Rule-15 phase 1 says
the plan exists first. The Operator's Y already fully specified the mutation (as #47/#48/#49's and
#57's openings did), so it stands as this row's disposition; the ordering fault is named here and
belongs in the incident row this d-work also owes (with #47/#58's merge-before-Y pattern) once a
PR of this d-work is open to carry it.
