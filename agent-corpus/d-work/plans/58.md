# Plan #58 — implement the survivor: sysadmin's authoring home moves here; fourth bundle row

Base commit of `main`: b13d67e. Refs #57 (the audit `agent-corpus/audits/2026-09-17-sysadmin-minimum.md`
is this plan's falsification; survivor A, attacks 1–9), #51–#55 (the enterprise installs that need
a host-administration front door).

## Intent as read
"falsify. implement survivor." The falsification is #57's audit: C1 (the release lacks sysadmin) is
true; C2 (it should carry one) is true for any installation with a host; the minimum craft already
exists at workstation. Survivor A: move sysadmin's authoring home to this repo, as sysarch (#160) and
syseng (#162) were moved from the same pre-split monorepo; ship it as the bundle's fourth row;
workstation installs it from the release and keeps tending its host in `workstation-corpus`.

## Mutation
Three PRs, one zone each (Rule-1), merged in order:
- **PR 1, craft zone** — `crafts/sysadmin/` = `pltrinh1122/workstation` `main`'s
  `crafts/sysadmin/` tree (38 files, VERSION 0.1.0), byte-for-byte (fetched by `gh api` tarball of
  that commit; the commit sha recorded in the PR and in `crafts/sysadmin/README.md` as the tree's
  origin, the way sysarch's README cites #160). `dyad craft check sysadmin` green. The kernel-only
  path with the craft present: its 4 guards register (`check --list`), its own tests run under
  Rule-12, the 7 core `needs_sysadmin` cases un-skip and pass, `dyad runbook check` no longer
  refuses, `agent/references` resolves the 13 `crafts/sysadmin/…` tokens and the 5 skipped kinds.
  Drift since the split (sysadmin 0.1.0 `requires: dyad-operator>=0.2.0`; the core is 0.6.0 — #39's
  `runbook_core.all_runbooks`, #37's `archived`, #6's modes) is fixed in this PR if any, each fix
  named in the completion reply, and `VERSION` → 0.1.1; if none, 0.1.0 stays. `crafts/REGISTRY.md`
  unchanged (authored → no row).
- **PR 2, infra zone** — `BUNDLE.md`: row `sysadmin | 0.1.x`; `version: 0.6.0 → 0.7.0` (a new
  component is a minor bump). `.github/workflows/dyad-package.yml`: the scratch-repo install covers
  every Tended craft in the tree if that is a one-line change; otherwise left for its own row
  (audit §6). `dyad bundle check` green.
- **PR 3, agent zone (docs)** — `README.md` "Two ways to pin a release": four components;
  `dyad/README.md` if it lists crafts. No Rule edit: Rules 8/18/19 and `dyad/CLAUDE.md` already
  name the craft at the path it will have.
- PR 2 depends on PR 1 (`bundle check` requires the row's craft in the tree); pushed `--no-verify`
  against a throwaway merge verified with `check --evidence` on the combined tree — the #21/#6/#37
  pattern — and merged after PR 1 under one Done-`Y`.
- **Not in this d-work:** the releases (`sysadmin-v0.1.x`, then bundle `v0.7.0` and
  `dyad-operator-v0.7.0`? — no: the core is unchanged, so only `sysadmin-v0.1.x` and `v0.7.0`; one
  `Y` per tag, Rule-11 Ratification events); the message to workstation (its own d-work: delete its
  authored tree, `dyad craft install` from the release); the backlog row for attack 3's Rule-8 gap.
  Each is asked after this d-work's Done-`Y`.

## Files touched
crafts/sysadmin/** (new, 38 files); BUNDLE.md; .github/workflows/dyad-package.yml (if one line);
README.md; dyad/README.md (if it lists crafts); agent-corpus/d-work/{rows,plans,provenance}/58.md

## Falsification
The audit's table is this plan's record (attacks 1–9). Two attacks specific to the mutation:
- "Take workstation's tree at its last *release* (`v0.3.1`), not its `main`" — refuted: no craft tag
  exists, and `v0.3.1` predates its later fixes; the survivor's scope (attack 7) is the current tree
  so nothing since the split is lost. The sha is recorded either way.
- "Bump the core to 0.7.0 too" — refuted: no byte of `dyad/` changes; Rule-11 p7 exists so a
  craft-only change bumps the bundle and never forces a core bump.
- Presence: `web-sysarch-adh151` files under `crafts/syseng/`, `dyad/guards/agent/{rows,sessions}.py`,
  `dyad/scripts/package.py` — no overlap with `crafts/sysadmin/`, `BUNDLE.md`, `README.md`. A
  `dyad-package.yml` edit is infra, claimed by no session. Host actions: none (the tarball lands in
  the scratchpad; the tree lands in the repo).
