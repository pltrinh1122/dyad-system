# Plan #51 — workstation#224 gap (a): Rule-14 manifest rows absent

Base commit of `main`: 41c6931. Refs workstation-224; siblings #52–#55.

Intake record (Rule-3 Intake), not an authorized plan: the row is `backlog`, opened on the Operator's `Y` (2026-09-16); no mutation is proposed until the Operator prompts for it. Source: workstation `main` `eb9322d`, `agent-corpus/d-work/plans/224.md` (row #224, "Cross-system reports") and `agent-corpus/falsification/224-boundaries.md`, resent verbatim by `dsv0.5.1: workstation` on 2026-09-16; verified by the sender against its installed core (dyad-operator 0.5.0, sysarch 0.1.2, syseng 0.1.2) — line numbers are that install's.

## Intent as read (the sender's observation, verbatim)
`dyad/infrastructure/INFRASTRUCTURE.md` has no row for the Anthropic API (Messages endpoint), the `anthropic` Python SDK, `msal` or `cryptography` (a PS256 client-credentials assertion for Entra app-only auth), `requests`, Microsoft Graph (OneDrive / Teams meeting transcripts), or systemd timers. Evidence: `grep -in "anthropic\|msal\|cryptography\|requests\|graph\|systemd" dyad/infrastructure/INFRASTRUCTURE.md` → no rows (the table's rows are Claude Code, Python, Git, bash+coreutils, GitHub, Gitea, Docker, GHCR, GitHub Actions, setup-python, curl, gh, jq, Linux). Mechanism verified: the manifest is core-craft content, byte-identical to the release and pruned on core install (Rule-11 p1), so an instance cannot add a row that survives the next install. Context: workstation #224's architecture names all six as future library rows (its Appendix B carries proposed six-cell rows: component, partition=library, version, purpose, license, replacement); none is invoked by any workstation code yet — the rows would be needed by an `extractor` craft not yet authored anywhere.

## Mutation
None proposed. Receiving Agent's reading at intake: by the sender's own account nothing invokes any of the six anywhere yet; Rule-14 adds a row when something invokes it, and the manifest guard warns on a row with no token. As filed this is a forward need of workstation's, not a defect in this tree — stated for the Operator, not disposed.

## Files touched
None yet.

## Falsification
Intake shape: defect-with-evidence (observation, path, reproduction, mechanism) — admissible. Whether it is a defect *here* is the reading above; the Operator decides when prompting.
