# Plan #168 — sync local git state with remote

Base commit of `main`: ae4d018 (after the fast-forward reported below). Refs #59, #140 (same operation).
Falsification: inline below (Rule-9).

## Intent as read
"sync with remote" — as #59 and #140: fast-forward, rebase what diverged, reconcile merged branches.

## Findings
- `main` was 102 commits behind `origin/main`, 0 ahead: clean fast-forward, done (not gated — a
  tracking-branch fast-forward changes which commits are checked out, not which exist; #59).
- No local branch besides `main`; `fetch --prune` removed nothing. Nothing to rebase or delete.

## Mutation
None beyond this ledger entry. No host action, no push of content, no PR.

## Files touched
agent-corpus/d-work/{rows,plans,provenance}/168.md

## Falsification
- "102 commits need review before syncing past them." Refuted on #140's precedent: sync is
  housekeeping, not an audit; a review is its own prompt.
- No honest counter found to the fast-forward itself.

## Incident (this d-work's opening)
First opened as #167; a peer session allocated #167 for different work in the same second, and
the push was rejected (add/add on `rows/167.md`), caught before anything of either reached
`main` twice. Reallocated from the fetched `origin/main`. The Rule-16 allocator race (#32) again,
caught by git's own non-fast-forward as `crafts/sysarch/rules/stores.md` now describes.
