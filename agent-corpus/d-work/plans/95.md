# Plan #95 — decision framing for P3 and P4 (ponytail proposal)

Base commit of `main`: 3bd3a83. Refs #93 (the proposal), #94 (the contingent adoption row).

## Intent as read
The Operator will dispose P3 (the check floor vs syseng's test mapping) and P4 (the `ceiling:`
marker) and wants, for each: the criteria the decision turns on, the options, and a score of my
recommendation against the alternatives — so the choice is informed, not deferred to me.

## Mutation (one agent-zone PR)
An addendum to `agent-corpus/audits/2026-09-18-ponytail.md`, "Part 2a — decision framing for P3
and P4": per item, the criteria (each with why it matters here and what evidence it reads),
three options, a 1–5 score per criterion with a stated weight, the total, and the recommendation
with its strongest counter. Measured inputs, not opinion, where a criterion can be measured: this
repo's test-to-module ratio (5,830 test lines to 6,803 module lines, 0.86 — the mapping is not
producing heavy suites), the `assert` ban (`syseng/invariants`: 0 asserts outside tests — an
option that puts a self-check in a script collides with it), the reference register (a marker
that cites a row resolves by one `REFERENCES` entry, no new store). For P4 the framing changes my
own proposal: the dyad-shaped marker cites a ledger row rather than becoming a second store.

## Files touched
agent-corpus/audits/2026-09-18-ponytail.md; agent-corpus/d-work/{rows,plans,provenance}/95.md

## Falsification
- "Scoring is theatre — weights encode the recommendation" — survives, mitigated: weights are
  stated and separable; the table is re-summable under the Operator's own weights, and each
  score cites its evidence line.
- "P4's revision should wait for the disposition, not change the proposal mid-review" — refuted:
  a framing that finds the proposal's own option weaker is the framing working; the original
  P4a+b stays in the table as an option, scored.
- Presence: no overlap. Host actions: none.
