# Plan #123 — incident mode G: mechanism defect (6 incidents: hook exec by path, --help as positional twice, presence rows, suites assuming crafts)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode G: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode G)
#1 (suites assumed the sysadmin/lan-git crafts); #13 (craft.py export --help wrote a file named --help); #6 (pre-commit execs containment.py by path, 644 -> exit 126); #25/#27/#35 (three --no-verify pushes while that hook was dead); #35 (session touch claims every open row); #116 (dwork new --help created a row titled --help)

## Falsification (candidate remediation)
#116's own incident is #13 recurring on a second subcommand: no subcommand of package.py parses flags before positionals. Candidate survivor: argparse subparsers for dwork new and craft export (help exits 0, writes nothing); the suite gains one test per subcommand asserting --help writes no file. Attack: widening the CLI is a Rule-12 path choice -- it is the mechanical check the Rule asks for.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/123-mode-G`): `agent-corpus/audits/2026-09-22-incident-mode-G.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-G.md`

## Amendment — 2026-09-22 (naming correction)
The record's file name is `agent-corpus/audits/2026-09-22-incident-mode-g.md`, lowercase:
`syseng/naming` requires the slug `[a-z0-9-]+` under `<instance>/audits/`, which the uppercase
mode letter this plan first named fails. Caught by the combined `check --evidence` run over all
ten branches before any merge; corrected in a second commit on this d-work's branch.
