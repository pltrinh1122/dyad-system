# Plan #121 — incident mode E: plan content wrong (3 incidents: record path, VERSION bump across zones, undeliverable item)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode E: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode E)
#17 (record path under the wrong root, two zones in one PR item); #22 (a craft VERSION bump dragging BUNDLE.md into a second zone); #32 (item 7 undeliverable under with-done sequencing)

## Falsification (candidate remediation)
Each plan named a path or zone without deriving it from containment.py zones or Rule-11's roots. Candidate survivor: a plan guard (agent/plans) that reads every plan file's 'Files touched' and fails when one Mutation item spans two zones or names a path under no zone. Attack: plan files are prose -- scoped: the guard reads the Files touched line only, one zone per listed group.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/121-mode-E`): `agent-corpus/audits/2026-09-22-incident-mode-E.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-E.md`

## Amendment — 2026-09-22 (naming correction)
The record's file name is `agent-corpus/audits/2026-09-22-incident-mode-e.md`, lowercase:
`syseng/naming` requires the slug `[a-z0-9-]+` under `<instance>/audits/`, which the uppercase
mode letter this plan first named fails. Caught by the combined `check --evidence` run over all
ten branches before any merge; corrected in a second commit on this d-work's branch.
