# Plan #43 — branch audit and pruning

Base commit of `main`: 4b67b1b. Read-only audit done; the pruning is the mutation.

## Audit (2026-09-16, after `git fetch --prune`)
Local: `main` only — clean. Tags: 10 releases, all kept (tags never move). Remote, 11 branches:
| branch | state | verdict |
|---|---|---|
| agent/1-core-suite, craft/1-suites | merged (PR #2, #1; 2026-09-14) | prune |
| agent/23-rule-disambiguation, agent/24-hook-interpreter | merged (PR #17, #18) | prune |
| claude/sysarch-backlog-filter-wat8mz, claude/syseng-pydantic-vs-zod-adh151 | merged (PR #14, and the pydantic PR) | prune |
| craft/22-sysarch-craft-filter, craft/27-failure | merged (PR #15, and #27's PR) | prune |
| infra/18-release-secret | unmerged, 1 ahead; PR #8 closed, superseded by #19 (my d-work #18) | prune |
| agent/25-containment-verbs | unmerged, 3 ahead; **PR #20 open** (web session's #25) | keep |
Every merged branch is a peer session's (mine were deleted at merge, `--delete-branch`); the web
session merges without deleting. Nothing on any of them is unreachable from `main` or a PR.

## Mutation
Delete the 9 remote branches marked prune (`git push origin --delete …`). A World action
(GitHub refs), not a host action; reversible in effect — every commit stays reachable from `main`
or from the closed PR #8's own record, and a deleted branch's PR keeps its history. No local branch
to delete. No repo transaction: no commit, no PR — the branch refs are not corpus.

## Falsification
- "Deleting a peer session's merged branches interferes with its work" — refuted: merged means
  `main` holds the commits; the web session's live branch (`agent/25-…`, open PR) is kept.
- "PR #8's branch should stay for the record" — refuted: PR #8 keeps its commits and diff after the
  branch is gone; the branch is the superseded attempt's scratch, not its record.
- Host actions: none. Presence: `web-sysarch` on #25 among others — its branch is the one kept.
