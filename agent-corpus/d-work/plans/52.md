# Plan #52 — workstation#224 gap (b): provenance.py credential shapes lack sk-ant-

Base commit of `main`: 41c6931. Refs workstation-224; siblings #51, #53–#55.

Intake record (Rule-3 Intake), not an authorized plan: the row is `backlog`, opened on the Operator's `Y` (2026-09-16); no mutation is proposed until the Operator prompts for it. Source: workstation `main` `eb9322d`, `agent-corpus/d-work/plans/224.md` (row #224, "Cross-system reports") and `agent-corpus/falsification/224-boundaries.md`, resent verbatim by `dsv0.5.1: workstation` on 2026-09-16; verified by the sender against its installed core (dyad-operator 0.5.0, sysarch 0.1.2, syseng 0.1.2) — line numbers are that install's.

## Intent as read (the sender's observation, verbatim)
`dyad/guards/agent/provenance.py:32-35` `FAIL_SHAPES` = GitHub token (`\bgh[pousr]_[A-Za-z0-9]{16,}`), GitHub PAT (`\bgithub_pat_[A-Za-z0-9_]{20,}`), private key (`-----BEGIN [A-Z ]*PRIVATE KEY-----`), Authorization header; `:36` `WARN_SHAPES` = bare 40-hex. No pattern matches an Anthropic API key (`sk-ant-api03-…`). Reproduction: a provenance body containing `sk-ant-api03-` followed by key-shaped text passes `dyad check --guards` (agent/provenance) with no fail and no warn.

## Mutation
None proposed until prompted. Receiving Agent's reading at intake: a real gap in Rule-7 property 2's residue check — an Anthropic key pasted into a chat and transcribed into a provenance body would be committed permanently; the fix's shape is one more `FAIL_SHAPES` entry plus its test. Recommended first of the five to prompt open.

## Files touched
None yet (expected: `dyad/guards/agent/provenance.py`, `dyad/tests/guards/agent/test_provenance.py`, Rule-7 Enforcement sentence, its falsification record).

## Falsification
Intake shape: defect-with-evidence — admissible.
