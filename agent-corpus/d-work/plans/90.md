# Plan #90 — dyad-operator 0.6.1 → 0.6.2; bundle 0.7.3

Base commit of `main`: 1baa405. Refs #68 (the drifting change: Rule-3 bullet + record), #71 and
#67 (same shape), #61.

## Intent as read
`dyad bundle build` at the `v0.7.2` tag (`b284769`) produced `dyad-0.6.1.tar.gz` `681e0f17…`; the
`dyad-operator-v0.6.1` release asset is `0c50d3d7…` (tag at `321b471`). `git log
dyad-operator-v0.6.1..v0.7.2 -- dyad/` = one commit, #68's (`RULE-3-d-work.md`,
`falsification/rules/rule-3-d-work.md`). This session changed the core craft an hour after
bumping it for the same fault and did not bump it again. Caught only because the rebuilt sha was
compared to the published asset before publishing; `v0.7.2` was tagged and pushed but never
released.

## Mutation
- Agent zone: `dyad/VERSION` 0.6.1 → 0.6.2 (patch: one Rule bullet and a record amendment).
- Infra zone: `BUNDLE.md` `dyad-operator | 0.6.2`, `version: 0.7.2 → 0.7.3`.
- Same two-PR shape as #71: infra depends on agent; throwaway-merge `check --evidence`; merged
  after Done-`Y`, after `dwork state 90 done`.
- Releases, one `Y` each after: `dyad-operator-v0.6.2`, `v0.7.3` — both cut at the *same* commit,
  and the bundle built from that commit, so every component archive equals its component tag's
  (verified by sha against the component releases before publishing).
- `v0.7.2`: left as a pushed tag with no release (the Operator gave no instruction to delete it);
  its absence from the releases page is the record. Named in the completion reply.
- Not here: the drift guard (the next d-work, before any further release).

## Files touched
dyad/VERSION; BUNDLE.md; agent-corpus/d-work/{rows,plans,provenance}/90.md

## Falsification
- "Publish v0.7.2 with the original 0.6.1 asset instead of the rebuilt one" — refuted: Rule-11 p7
  builds the bundle from the bundle tag's tree by one code path; hand-assembling assets from other
  releases is a second mechanism, and BUNDLE.sha256 would not be reproducible from the tag.
- "Skip 0.6.2; fold #68's change into the next core release whenever that is" — refuted: until
  then `main`'s `dyad/` answers to a version whose archive it does not match — the defect itself.
- Presence: no session names `dyad/VERSION` or `BUNDLE.md`; `web-sysarch-adh151` rows are craft
  files. Host actions: none.
