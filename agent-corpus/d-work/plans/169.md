# Plan #169

Rebuild the three dyad-system PRs of #152 whose merge the Done-Y of #152 could not execute.

Intent as read: land #152's ratified dyad-system content (PR #174 `.claude/*` zone + suite_env fix + core 0.9.1, PR #175 bundle row, PR #176 countersign 0.2.0) on current `main`, which moved; the Done-Y bound only the old heads (Rule-2 Binding), so new heads need a new Done-Y.

Mutation:
- Rebuild branches `agent/152-claude-zone`, `infra/152-bundle`, `craft/152-countersign-0.2` on current `main`, re-authored as the session identity (the old heads carry the fixture identity `t <t@t>` written by the leak), commit messages citing d-work #169.
- `INCIDENTS.md`: keep #161's rows, then #152's row, amended with the identity damage, the three `t@t` commits already on `main` (6ba6efb, f35dbe7, 0fb8bc7) and row #165.
- `dyadlib.GIT_VARS` gains `GIT_COMMON_DIR` (#161's finding); `SuiteEnvTests` asserts it.
- Fresh `dyad check --evidence` on the new heads; then `Y/N: Done with #169 …` naming them.

Files touched: dyad/guards/infra/containment.py, dyad/tests/guards/infra/test_containment.py, dyad/falsification/rules/rule-1-containment.md, dyad/scripts/package.py, dyad/scripts/dyadlib.py, dyad/tests/test_package.py, dyad/VERSION, agent-corpus/audits/INCIDENTS.md, BUNDLE.md, crafts/countersign/**.

Base commit: 4a511e9.

Falsification: see plan 152 (R1–R7) and the #152 amendment in the Rule-1 record; the added GIT_VARS entry is attacked by SuiteEnvTests and the bait-clone run. Closes backlog row #165's defect.
