# Plan #53 — workstation#224 gap (c): manifest import scan never walks crafts/

Base commit of `main`: 41c6931. Refs workstation-224; siblings #51, #52, #54, #55.

Intake record (Rule-3 Intake), not an authorized plan: the row is `backlog`, opened on the Operator's `Y` (2026-09-16); no mutation is proposed until the Operator prompts for it. Source: workstation `main` `eb9322d`, `agent-corpus/d-work/plans/224.md` (row #224, "Cross-system reports") and `agent-corpus/falsification/224-boundaries.md`, resent verbatim by `dsv0.5.1: workstation` on 2026-09-16; verified by the sender against its installed core (dyad-operator 0.5.0, sysarch 0.1.2, syseng 0.1.2) — line numbers are that install's.

## Intent as read (the sender's observation, verbatim)
`dyad/guards/infra/manifest.py:148` `python_dirs(pkg=dyadlib.PKG)` lists `pkg/scripts`, `pkg/guards/<corpus>/`, `pkg/tests/`, `pkg/tests/guards/<corpus>/` — all under `dyad/`; `:154` `python_imports(pkg)` walks only `python_dirs(pkg)`; the sole caller is `scan()` (`:185`, which additionally reads the repo-root `.github/workflows/dyad-*.yml` for tokens, still nothing under `crafts/`). Reproduction (live, this checkout): `python3 -c "import sys; sys.path.insert(0,'dyad/scripts'); import dyadlib; print(dyadlib.load_guard('infra','manifest').python_imports())"` → `{}`. `dyad/guards/infra/manifest_rules.txt` has exactly one `import:` mention, line 4, a comment documenting the token form; zero live `import:` tokens. Consequence verified by construction: a Tended craft's third-party Python import (none exists here today; workstation's crafts are stdlib-only) would never be flagged as undeclared. Related: no instance-local manifest overlay exists — the containment guard has `package_rules.local.txt` (workstation #192), the manifest guard has no counterpart.

## Mutation
None proposed until prompted. Receiving Agent's reading at intake: consistent with Rule-11 p5/p1 (every Tended craft's guards are discovered; its imports should be scanned the same way — `dyadlib.craft_glob` is the existing primitive). The overlay remark is a separate proposal, not part of this defect.

## Files touched
None yet (expected: `dyad/guards/infra/manifest.py`, its tests, `crafts/syseng/rules/imports.md` property 4 wording).

## Falsification
Intake shape: defect-with-evidence — admissible.
