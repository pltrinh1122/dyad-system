# Plan #31 — explicitly admit pydantic to the kernel (Rule-14 property 2)

Base commit of `main`: 7bd43ce (re-fetched; refs #30, whose reconciliation named exactly this fork).
Falsification: inline below (Rule-9 Conditions: an Operator prompt proposing a claim a Rule would encode).

## Intent as read
The Operator disposes the fork #30's own reconciliation left open: "if the Operator disposes that
the kernel-only path may require a `pip install`, pydantic becomes the right answer." The prompt
asks for exactly that disposition, named for pydantic specifically — not a general license for any
package to nominate itself.

## Falsification
| # | Attack | Result | Survivor |
|---|--------|--------|----------|
| 1 | Kernel (Rule-14 p2) is explicitly "minimal" and today is three items, none needing an install beyond what's already there; a fourth, third-party PyPI package changes kernel from zero-install to "run one `pip install` first," eroding property 3's basis. | Survives, scoped | Python itself already requires an install step matched to a pinned version — kernel was never "nothing to set up," only "nothing beyond {agent, Python, git}, pinned and observed." Extending that discipline to one named, pinned package is a difference of degree, not kind, and is scoped to pydantic by name — not a standing invitation for the next library to self-nominate. |
| 2 | `pydantic-core` is a compiled Rust wheel; a compiled dependency has no place in a kernel meant to run "on the kernel alone." | Refuted | Both non-Python kernel members are themselves compiled (CPython, git). "No compiled code" was never the real constraint; property 3's actual constraint is reproducibility without a library/hosting adapter — installable, pinned, offline-capable once installed, exactly as Python's own interpreter is. |
| 3 | This reverses #30's own recommended path (a stdlib emitter) without amending that record. | Confirmed | #30's reconciliation named this precise fork as the Operator's to decide. The record is not rewritten — it stands as the reasoning that led here — but gains a one-line pointer to this amendment so it isn't read as still-current advice against pydantic. |
| 4 | Nothing in the corpus imports pydantic yet; naming it kernel before any code uses it is premature. | Survives, scoped | Classification and adoption are separate acts — Rule-12 still chooses the implementation path when code is actually written. This d-work only clears the kernel-membership question. The manifest row pins a real, current release (2.13.5, checked via `pip index versions pydantic` today) rather than a placeholder, so the declaration is falsifiable against an actual artifact before anything imports it. |

No honest counter beyond these four found.

## Mutation
Agent-zone PR citing `d-work #31` (Rule-1: package-root manifest and Rule text are agent zone):
1. `dyad/rules/RULE-14-system-infrastructure.md` — property 2 gains "and pydantic at a pinned
   version — the one third-party package the kernel-only path may import directly." No other
   property or Enforcement text changes: property 3 already reads "the kernel alone," which now
   includes pydantic; the manifest guard already accepts `kernel` as a partition value generically
   (no code change).
2. `dyad/infrastructure/INFRASTRUCTURE.md` — one new row: `pydantic | kernel | 2.13.5 pinned (not
   yet installed; adopted when a plan first imports it) | data-model schema definition, validation
   and JSON Schema emission for corpus entities (agent-corpus/falsification/schema-definition-language.md,
   d-work #30) | MIT | a stdlib JSON Schema emitter over the existing guard contract (dyadlib.FIELDS
   + describe())`.
3. `dyad/vocabulary/VOCABULARY.md` — the `kernel` row's definition gains "and pydantic" to match
   Rule-14 p2 (Rule-6 master record: on divergence the vocabulary wins and the Rule is a bug; this
   keeps the two from diverging in the first place).
4. `dyad/falsification/rules/rule-14-system-infrastructure.md` — amendment for d-work #31: the four
   attacks above, continuing the file's numbering (13–16), with the Rule-5 pairwise statement.
5. `agent-corpus/falsification/schema-definition-language.md` — one line at the end of attack 6's
   row area pointing to this amendment: the kernel-reproducibility cost that attack named is now
   resolved by disposition, so pydantic (not the stdlib emitter) is the live recommendation for any
   future d-work that actually writes the schema code.

Not in this d-work: writing any pydantic-based code (the emitter/model itself); installing pydantic
on this or any host (no host action — the manifest row documents the pin, it does not run `pip
install`); editing `crafts/sysarch/rules/manifest.md`'s own paraphrase of the kernel list, which is
craft zone and now stale by the same margin — noted here, left for its own craft-zone PR (Rule-1:
change the referent, Rule-14, before the referrer, the craft rule).

## Files touched
dyad/rules/RULE-14-system-infrastructure.md; dyad/infrastructure/INFRASTRUCTURE.md;
dyad/vocabulary/VOCABULARY.md; dyad/falsification/rules/rule-14-system-infrastructure.md;
agent-corpus/falsification/schema-definition-language.md;
agent-corpus/d-work/{rows,plans,provenance}/31.md
