# Plan #69 — falsify "dyad-system upgraded to 0.7.1"; record the release-step gap

Base commit of `main`: b3d0ad4. Refs #67 (the d-work that bumped the version files), #68 (a peer
session's own, distinct finding on the same three d-works). Falsification: inline below.

## Intent as read
"dyad-system upgraded to 0.7.1. falsify." — check the claim by Rule-9's own method, not by reading
`BUNDLE.md`'s one line and accepting it.

## Finding
Six attacks (reproduced in the reply that opened this d-work), all failed to sustain the claim as a
release. Verified directly: `git tag -l` has no `v0.7.0`, `v0.7.1`, `dyad-operator-v0.6.1`,
`sysarch-v0.1.5`, or any `sysadmin-v*` at all; the bundle's own tag history stops at `v0.5.1`,
`dyad-operator` at `v0.5.1`, `sysarch` at `v0.1.2`. `dyad-release.yml` triggers only on a pushed tag
or a dispatch naming an existing one — nothing in the pipeline can substitute for the `Y/N: release
<tag>?` Rule-11 requires. Row `#67`'s own plan named three release counter-prompts "asked separately
after Done-`Y`"; `#67` is `done` and none of the three appears in its `disposed` column or its
provenance. The step the plan named was never executed — not skipped by decision, just never asked.

Survivor: "dyad-system upgraded to 0.7.1" is false as a release claim. What's true is narrower — the
bundle manifest and four `VERSION` files were edited to a mutually consistent 0.7.1/0.6.1/0.1.5/
0.1.3/0.1.0, and the bundle guard (a bookkeeping check only) reports that consistency as `ok`. No
tag, archive, or GitHub Release exists at any of those numbers for any of the four components.

## Mutation
One PR, **agent zone**, citing `d-work #69`:
1. `agent-corpus/audits/INCIDENTS.md` — one row: the release step `#67`'s own plan named was never
   asked, so `BUNDLE.md`'s version line is unreleased bookkeeping, not a shipped upgrade.

**Not in this mutation, stated:**
- No tag is cut here. Whether to release `v0.7.1` (and the two craft tags under it) is the
  Operator's disposition, not this d-work's — falsifying the *claim* is not authorizing the
  *release*, and the ratification a tag needs (Rule-11) is a separate ask this d-work does not make.
- `#68`'s own finding (self-ratified merges on `#47`, `#58`, `#67`) is not restated or re-verified
  here; it is a different fault (how those merges landed) on the same three d-works, already being
  recorded by the session that found it. This d-work's row cites the release-step gap only, and
  cross-references `#68` rather than duplicating it.

## Files touched
agent-corpus/audits/INCIDENTS.md;
agent-corpus/d-work/rows/69.md; agent-corpus/d-work/plans/69.md; agent-corpus/d-work/provenance/69.md

## Falsification
Reproduced from the reply that opened this d-work; six attacks, results as given there:
1. The `BUNDLE.md` line and a green guard are enough. Refuted as sufficient — the guard checks
   internal consistency, never that a release happened; the vocabulary defines release as a tag.
2. The bundle was tagged at 0.7.1, or 0.7.0 before it. Refuted — no such tags exist; the bundle's
   tag history stops at `v0.5.1`.
3. Its components were tagged individually even if the bundle wasn't. Refuted — `dyad-operator` and
   `sysarch` tags are both behind their `VERSION` files by multiple bumps.
4. `sysadmin` shipped as part of this bundle release. Refuted — zero `sysadmin-v*` tags exist ever.
5. The release workflow will pick this up automatically. Refuted — it triggers only on a pushed tag
   or a dispatch naming an existing one; nothing automates the Operator's own ratification.
6. This is an isolated, fresh defect. Refuted, and compounded — `#67`'s own plan scheduled the
   release counter-prompts and they never fired; `#68` (a peer session) independently found the
   merges landing this same version bump were themselves under-ratified. Two distinct faults on the
   same three d-works, not one.
- **Overlap (Rule-16):** presence read before writing this plan. `dev-main` (stale) claims rows
  including `#56/#57`, none of which this plan touches. `web-sysarch` (live, same root) claims
  `#56 #58`, `files=0` — no file declared. This plan writes only `agent-corpus/audits/INCIDENTS.md`
  and its own ledger files; no overlap found.
