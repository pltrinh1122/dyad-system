# Plan #69 — falsify "dyad-system upgraded to 0.7.0"

Base commit of `main`: b3d0ad4 (current tip after #59's own housekeeping). Refs #58, #61, #67
(the release history the claim touches), #68 (a peer's already-planned, adjacent finding).
Falsification: this whole plan is the record (Rule-9: an Operator prompt asserting a claim).

## Intent as read
Falsify one sentence: "dyad-system upgraded to 0.7.0." No "implement survivor" was asked this
time, so this plan's mutation is the falsification record and, where a **new**, not-yet-caught
gap surfaces, a backlog row naming it — never a silent fix, never touching a d-work already
`planned` under someone else's authorization (#68, below).

## Attacks

**A1 — "upgraded to 0.7.0" names the current state.** **Confirmed false, by inspection of the
tags:**

| tag | commit date | note |
|---|---|---|
| `v0.7.0` | 2026-09-17 17:10:06 | cut as part of #58 |
| `v0.7.1` | 2026-09-17 17:41:55 | cut 31 min later, as part of #67 — `git merge-base --is-ancestor v0.7.0 v0.7.1` confirms it is a strict descendant |

The bundle's current `version:` on `main` right now is **0.7.1**, not 0.7.0. Survivor: the system
upgraded *through* 0.7.0 to 0.7.1; naming 0.7.0 as the destination is one release behind.

**A2 — the 31-minute gap between the two tags is incidental (a quick patch, nothing wrong with
0.7.0 itself).** **Refuted.** Plan #67's own text: `dyad/VERSION` and `crafts/sysarch/VERSION`
had been static at `0.6.0`/`0.1.4` while their trees kept changing (#45, #47, #50, #58 all landed
on top without a bump) — verified again here, independently: `git diff --stat
dyad-operator-v0.6.0..v0.7.0 -- dyad/` is non-empty against a `VERSION` that never moved across
that diff. **`v0.7.0`'s own `BUNDLE.md` rows (`dyad-operator: 0.6.0`, `sysarch: 0.1.4`) understated
what was actually in the tree at the moment that tag was cut.** `v0.7.1` is not a quick patch on a
sound release; it is the correction of a versioning defect in `v0.7.0` itself, found and fixed
inside the same working session that cut it.

**A3 — the defect A2 names is closed, nothing left to check.** **Survives, scoped, checked live
just now:** `git diff --stat <tag>..main -- <tree>` is **empty** for all three released crafts
against their current tags —

    dyad-operator-v0.6.1 vs dyad/            -> no diff
    sysarch-v0.1.5       vs crafts/sysarch/  -> no diff
    sysadmin-v0.1.0      vs crafts/sysadmin/ -> no diff

So there is **no live drift right now** — the survivor of A2 fixed what it targeted. What survives
is process, not content: plan #67 itself deferred "a mechanical guard against this recurring" as
"its own d-work, named in the completion reply, not designed here." No such row is visible in this
corpus (searched `agent-corpus/d-work/rows/*.md` for it). Whether it was named in that session's
own chat reply (outside the corpus, Rule-7's own stated boundary) is **not something this plan can
verify** — stated as unverifiable, not asserted as a breach.

**A4 — row #61 (`backlog`, "surfacer#242 finding (i): sysarch-v0.1.4 release archive predates the
#50 merge") is now moot, since #67 bumped past it.** **Refuted as "moot," survives as "root-caused
but not closed."** #67's fix stops the *recurrence*; it does not and cannot touch `sysarch-v0.1.4`
itself — Rule-11 property 4 and its own provenance line are explicit that tags never move. Anyone
who already pinned `sysarch-v0.1.4` keeps the stale archive `#61` found, permanently, by design.
#61 is still sitting `backlog`, unreferenced by #67 or #68, describing the exact defect class #67
just fixed for the *next* occurrence. Detected, not disposed: it is not this plan's row to
reclassify.

**A5 — the release-tag ratification itself (Rule-11: "Answering `Y` to a release counter-prompt,
`Y/N: release <tag>?`," a ratification event distinct from a d-work's Done-`Y`) is on record for
these five tags.** Checked, not assumed: `provenance/58.md` and `provenance/67.md` each hold
exactly two disposition entries, matching their rows' `disposed` columns (plan + done) — no third,
distinctly-worded release disposition for `sysadmin-v0.1.0`/`v0.7.0` (#58) or
`dyad-operator-v0.6.1`/`sysarch-v0.1.5`/`v0.7.1` (#67) appears anywhere in the corpus. **This is
not the same finding as #68** (already `planned`, unrelated to my prompt, catching *PR merges*
executed before their Done-`Y` was recorded) — #68's three occurrences are #47, #58, #67's *merge*
timing; A5 is about the *release tag* ratification Rule-11 names as its own event, which #68's plan
does not mention. Whether the release question was asked and answered outside the provenance
record (a Rule-7 completeness gap only, since Rule-7 property 5 checks the disposition *count*
matches the row's, and both already do, at 2 — so no mechanical check would ever catch this
either way) or skipped entirely (a Rule-2/Rule-11 breach) **cannot be determined from the corpus
alone.** Recorded as a finding for the Operator to confirm from the Operator's own copy of the
chat (Rule-7 Enforcement: "the Operator's own copy of the chat is the other" check on a clerical
entry's faithfulness) — never resolved by inference here.

## Survivor
"Upgraded to 0.7.0" survives only as "upgraded *through* 0.7.0, which needed an immediate
version-accounting correction, to the actual current release, 0.7.1." No live drift exists right
now (A3). #61 (backlog) and A5 (a new finding) are both real, both outside this plan's authority
to fix unilaterally, and both worth the Operator's attention.

## Mutation — one PR, agent zone
1. `agent-corpus/falsification/dyad-system-0.7-release-claim.md` — this record, in the standard
   attack/result/survivor table form (Rule-9).
2. One new backlog row: "no distinct `Y/N: release <tag>?` disposition is recorded for
   sysadmin-v0.1.0, v0.7.0, dyad-operator-v0.6.1, sysarch-v0.1.5 or v0.7.1 (Rule-11 Ratification
   events); confirm against the Operator's own chat record, per Rule-7 Enforcement, before
   deciding whether this is a gap in the record or in the conduct" — `refs: 58 67 68`.
Not touched: #61 (someone else's backlog row; flagged in the record, not reclassified), #68
(already `planned` under its own author's Y; not mine to widen or re-scope).

## Files touched
agent-corpus/falsification/dyad-system-0.7-release-claim.md;
agent-corpus/d-work/{rows,plans,provenance}/69.md; one new row for the A5 finding.

## Falsification of this plan's own choice not to fix anything
- **"A5 is exactly what #68 should absorb — open a duplicate is waste."** Refuted: #68 is
  `planned` (its plan-`Y` already binds its PR to its own stated scope, three PR-merge-before-
  Done-`Y` occurrences); editing it to add a fourth, differently-shaped finding after its `Y` was
  given would be a departure from #68's own authorized plan, which Rule-3 says needs its own
  author's new plan-`Y`, not a bystander's addition.
- **"Silently correcting the Operator's '0.7.0' with '0.7.1' in a reply is enough; no record
  needed."** Refuted: the Operator's own word was "falsify," which Rule-9's Form requires as a
  written record in `agent-corpus/falsification/`, not a one-line correction that leaves no trace
  for the next session to find.
- Overlap (Rule-16): `web-sysarch-adh151`'s presence file lists files under `crafts/syseng/`,
  `dyad/guards/agent/{rows,sessions}.py`, `dyad/scripts/package.py` — none touched here. No
  overlap.
