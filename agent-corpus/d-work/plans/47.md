# Plan #47 — plan.base->commit skips done/archived rows (parent #49)

Base commit of `main`: cfc1d55. Refs #48 (sibling: dyad/tests flakiness, same parent), #49 (parent).

## Intent as read
`agent-corpus/d-work/plans/19.md` records `Base commit of main: 49e9988`, the tip of
`infra/18-release-secret` (PR #8), closed unmerged (the plan's own text: "superseded twice in one
thread"). The v0.5.1 release run (10:53 today) shows `check and build`: success on a fresh
checkout; the v0.6.0 run (21:24) fails on it — the branch was deleted in between, and the commit,
never an ancestor of `main`, became unreachable from any ref. It still resolves in this session's
own long-lived clone only because that clone fetched the branch before deletion — a fossil no
fresh clone can have. Confirmed independently against a fresh `git clone` of `origin`.

The field's only operational purpose is Rule-15 phase 2: "If `main` has moved past a touched file,
the Agent re-plans" — a drift check that only ever fires while a row is still active (`open`,
`planned`, `blocked`). Once a row is `done`, nothing re-plans against it again; the base commit is
frozen history, not a live reference. Rule-20's resolver currently checks it forever regardless of
row state — the actual bug.

## Mutation
- `dyad/guards/agent/references.py`: `Corpus.__init__` gains `self.row_states = {r.id: r.state for
  r in dyadlib.read_rows(root)} if dyadlib.rows_dir(root).is_dir() else {}`. `base_commit(c)` skips
  a plan whose row is `done` or `archived` (looked up by `int(p.stem)`); `open`/`planned`/`blocked`
  rows, and any plan with no row file at all, are checked exactly as today.
- `dyad/rules/RULE-20-referential-integrity.md` property 2: one sentence beside the existing
  "skips its kinds with a printed line, never a failure" naming this per-row exemption — a
  different shape (one kind, some instances) from the existing per-kind skip.
- Falsification: amendment to `dyad/falsification/rules/rule-20-referential-integrity.md`, in the
  style of its existing #177 amendment (attack, disposition, tests named, Rule-5 pairwise
  restated); written in full as part of the PR, not here.
- No change to `plans/19.md` itself or any other historical plan file — frozen history stays frozen
  (Rule-7's verbatim-record spirit extends to plan files: the fix is in the resolver, never in the
  record).
- Tests: `dyad/tests/guards/agent/test_references.py` — a fixture with two rows sharing an
  unresolvable base commit, one `open` (still fails) and one `done` (now skipped, no fail); the
  existing 597-reference/15-kind counts are unaffected (the kind itself is unchanged, only its
  per-instance filter).

## Files touched
dyad/guards/agent/references.py; dyad/rules/RULE-20-referential-integrity.md;
dyad/falsification/rules/rule-20-referential-integrity.md; dyad/tests/guards/agent/test_references.py;
agent-corpus/d-work/{rows,plans,provenance}/47.md

## Falsification
- "Special-case #19's plan file (edit or strike the line)" — refuted: fixes nothing for the next
  closed-unmerged-branch plan, and rewrites frozen history for no structural gain.
- "Downgrade plan.base->commit to warn-always, like a `world` kind" — refuted: throws away the real
  case too — an *active* d-work's base commit going stale mid-execution is exactly what Rule-15
  phase 2 needs caught; warn-always lets that slide silently as well.
- "A done/archived row's base commit could still hide a genuine typo, now never caught" — survives,
  scoped, traded away openly: the real defense for an active row was always Rule-15 phase 2's
  re-plan check plus the Operator's Done-`Y` at closing time; existence-only checking never verified
  *content* correctness in the first place (the same trade this record's own #177 amendment already
  made for package-side citations).
- Presence: `web-sysarch` (rows 22–25,27,33–35,42,43) and `web-sysarch-adh151` (row 32, files
  `dyad/guards/agent/{rows,sessions}.py`, `dyad/scripts/package.py`, `crafts/sysarch/rules/stores.md`)
  — neither touches `dyad/guards/agent/references.py` or its test. Host actions: none.
