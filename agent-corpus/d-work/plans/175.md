# Plan #175

Intent as read: let an instance name where its host and infrastructure records live and which zone
holds them, so countersign-system can use `infrastructure/` in zone `infra` (four zones), without
changing anything for dyad-system or its client. Prerequisite of countersign-system #2 (revision 3);
the Operator's `Y` to "adopt infrastructure/ … re-plan #2 … with the dyad-system core prerequisite
… planned first" is this row's first provenance entry (relayed via countersign-system-2).

Mutation:
- Two preferences (Operator-owned, instance): `host-path` (default `workstation-corpus`) and
  `host-zone` (`workstation` | `infra`, default `workstation`). Durable per instance, unlike an
  environment variable (hooks, CI and a new session all read the tree, not the shell).
- Core (agent zone): `dyadlib.host_path(root)` / `host_zone(root)` read the preferences (env override
  `DYAD_HOST` for tests, as `DYAD_INSTANCE`); `containment.ZONES` computes its host row from them and
  drops `workstation` from the zone set when `host-zone` is `infra`; `references.py`, `rows.py`,
  `runbook.py` and `crafts.py` stop hard-coding `workstation-corpus`; a guard's logical corpus
  `workstation` resolves to the host zone. Rule-1 gains one sentence (the host row is instance data
  read from the preferences); its falsification record gains the amendment; vocabulary if a term is
  added. Core 0.9.1 → 0.10.0 (new preferences).
- Crafts (craft zone, one PR): sysadmin guards (`ops_scripts`, `runbooks`, `changelog`, `events`),
  sysarch projectors (`project_schema`, `project_instances`), syseng `naming_rules.txt` read the host
  path instead of the literal; each craft bumps a minor version.
- Preferences (preferences zone): dyad-system's `PREFERENCES.md` gains both keys at today's values.
- Infra: `BUNDLE.md` rows follow the versions.
- Evidence: with the defaults, `containment.py zones` and every guard's output are byte-identical to
  today's on dyad-system (a test pins it); a scratch instance with `host-path: infrastructure`,
  `host-zone: infra` classifies `infrastructure/*` as `infra` and has four zones.

Files touched: dyad/scripts/dyadlib.py, dyad/guards/infra/containment.py, dyad/guards/agent/references.py,
dyad/guards/agent/rows.py, dyad/scripts/runbook.py, dyad/guards/craft/crafts.py, dyad/rules/RULE-1-containment.md,
dyad/falsification/rules/rule-1-containment.md, dyad/VERSION, their tests; crafts/sysadmin/**, crafts/sysarch/projectors/**,
crafts/syseng/guards/naming_rules.txt and their VERSIONs; preferences-corpus/PREFERENCES.md; BUNDLE.md.

Base commit: b8f7dae.

Falsification:
| # | attack | result |
|---|---|---|
| F1 | an environment variable (like `DYAD_INSTANCE`) suffices | Refuted: hooks, hosted CI and a fresh session read the tree, not the shell; countersign-system would silently fall back to `workstation-corpus` |
| F2 | a preference is the wrong owner for zone data | Survives, with cost: Rule-1 must say it reads them, a Rule edit with its pairwise statement (Rule-5); the zone *table* stays owned by `containment.py`, only its host row becomes instance data |
| F3 | dyad-system or its client changes | Refuted by construction, to be proven: defaults reproduce today's table and paths byte-for-byte, pinned by a test and by the evidence block |
| F4 | `infrastructure/*` collides with another pattern | Refuted: disjoint from every row, including `dyad/infrastructure/*` (under `dyad/*`) and `.claude/*`; `zone-patterns-disjoint` checks it |
| F5 | the change stays inside the core | Refuted: three Tended crafts hard-code the path, so a craft-zone PR and craft version bumps are needed |
| F6 | `ZONES` can stay a constant | Refuted: it becomes computed at import from the tree's preferences; the invariants run over the computed table |
| F7 | removing a zone breaks guards that declare `CORPUS = "workstation"` | Survives as the main risk: the logical corpus resolves to the host zone; tested with both settings |
| F8 | presence overlap | to check at plan-Y against every session's presence file (Rule-16) |
