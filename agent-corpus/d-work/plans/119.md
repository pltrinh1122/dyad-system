# Plan #119 — incident mode C: concurrent-session races (3 incidents: presence identity, id allocation)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode C: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode C)
#30 (peer's #23 executed twice; id taken twice, #24->#25->#30); #26/#34 (id assumed before the allocator printed it, peer's #27 overwritten); #27 (provenance written as 26.md)

## Falsification (candidate remediation)
Owned by backlog #32 (name collision, id collision, presence claiming every row). This child adds the count and refs #32; its mutation is the mode file only. Attack: a third file on the same defects -- refuted: this is the audit's per-mode record, #32 stays the remediation row.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/119-mode-C`): `agent-corpus/audits/2026-09-22-incident-mode-C.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-C.md`
