# Plan #18 — release workflow uses repo secret RELEASE_TOKEN

Base commit of `main`: d144412. Refs #13, #16 (the release this unblocks).

## Intent as read
`gh release create` inside `dyad-release.yml` 403s ("Resource not accessible by integration") on
the automatic `github.token` despite explicit `permissions: contents: write`; two attempts (the tag
push, a re-dispatch after the Operator raised the repo's default workflow permission to write)
failed identically, so the cause is not the repo default. Operator-chosen path (AskUserQuestion,
prior reply): a fine-grained PAT as a repo secret. The Operator minted one and it is now set as the
Actions secret `RELEASE_TOKEN` (`gh secret set`, confirmed via `gh secret list`; the raw value was
never written to any file in this corpus, Rule-7 property 4 read against not committing a live
credential — provenance #18 records the redaction and why).

## Verified (read-only)
`gh secret list` shows `RELEASE_TOKEN` (just set) and a second, unexplained secret `RELEASE_SECRET`
(present already, no reference to either name anywhere in the tracked tree). Not used here — its
origin is unknown; surfaced to the Operator, not touched (Rule-8 conduct: detect, don't dispose).
A stray `workflow_dispatch` run (34984664908) fired before this plan was fully written — a dispatch
API call made in the same turn as setting the secret, before wiring the workflow to it — expected
to fail the same 403; harmless, no action taken.

## Mutation
One infra-zone PR: `dyad-release.yml`'s Release step — `env: { GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}" }`
in place of `${{ github.token }}`. Nothing else changes (check/build steps, triggers, the tag/VERSION
check). Then re-dispatch `v0.3.2` (clerical execution of the release Y already given under #13) and,
once confirmed, offer the same for the two craft tags.

## Files touched
.github/workflows/dyad-release.yml; agent-corpus/d-work/{rows,plans,provenance}/18.md

## Falsification
- "The secret might not actually carry contents:write" — falsified by running it: the dispatch
  after this merges either succeeds or names a different error; either way it is observed, not assumed.
- "Reuse the existing RELEASE_SECRET instead of a new name" — refused: unknown provenance, unknown
  scope; using an unidentified credential without the Operator confirming its origin is the wrong
  default. Survivor: RELEASE_TOKEN, freshly minted for this purpose, named plainly in the workflow.
- Host actions: none (GitHub Actions secret, not the local host — The World, Rule-14, not Rule-8).
  Presence: no other session file.
