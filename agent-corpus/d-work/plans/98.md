# Plan #98 — workstation#247 defect: sysarch ENTITIES hardcodes contributing crafts

Base commit of `main`: 9003180. Refs workstation-247, #46 (the same shape, fixed for
`KNOWN_CRAFTS` in `dyad/tests/test_package.py`, this file untouched by it).

Intake record (Rule-3 Intake), not an authorized plan: the row is `backlog`, opened on the
Operator's `Y`; no mutation until prompted.

## Intent as read (the sender's observation, verbatim as carried)
`crafts/sysarch/tests/test_project_entities.py` line 164:
```
ENTITIES = ({... core ...} | ({"changelog","command","event","ops"} if SYSADMIN else set())
            | ({"image"} if LANGIT else set()))
```
while `G` (line 12) is built from `dyadlib.guard_files()` — every installed craft's guards. A
craft with guards that is not sysadmin or lan-git breaks four tests:
`test_one_entity_per_guard_sorted_each_with_fields` ("Items in the first set but not the second:
'write set' 'output' 'instruction' 'profile'"), `test_render_deterministic_escaped_and_
self_contained`, `LiveTests.test_empty_instance_changes_nothing`, `LiveTests.test_live_run`.
Reproduces at sysarch 0.1.5 with workstation's `crafts/automaton/` present (four guards:
`instructions.py`, `outputs.py`, `profiles.py`, `scope.py`, each meeting `dyadlib.CONTRACT`;
`dyad craft check automaton` ok; `check --guards` green). Same shape as `KNOWN_CRAFTS` in
`dyad/tests/test_package.py` before d-work #46 derived unknown crafts' guards from
`crafts/<name>/guards/*.py`. Not patched by the sender (installed craft, not authored there); the
four reds are named in workstation's own #247 evidence.

## Mutation (when prompted)
Craft zone: `ENTITIES` derived from `G`'s own keys the way `#46` derived `KNOWN_CRAFTS`, dropping
the `SYSADMIN`/`LANGIT` flag pair; the four named tests re-verified against a fixture that adds a
third contributing craft (mirroring how `#46`'s fix was tested), so this exact class doesn't
recur a third time. `crafts/sysarch/VERSION` bump (the drift guard, #91).

## Files touched
None yet.

## Falsification
Intake shape: defect with evidence (path, line, four named failing tests, reproduction, mechanism
verified) — admissible.
