# Plan #118 — incident mode B: stale local view of the remote (5 incidents: unfetched refs, stale local main)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode B: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode B)
#3; #22 (unfetched branch tip read as an unrecoverable sha); #22 (branches cut from a 4-behind main); #80 (tags never fetched); #110 (git checkout main -- . on a 328-behind local ref)

## Falsification (candidate remediation)
Every case followed an explicit refspec fetch or none at all. Candidate survivor: dyad session touch runs git fetch --tags --prune origin first and prints main's distance, so the first command of every session and every plan-Y refreshes the view; and the local main ref is never used bare (#110). Attack: a fetch at touch time is stale by execution time -- conceded; the fetch belongs before any claim that a ref does not exist, stated as the rule learned in #22 and #80.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/118-mode-B`): `agent-corpus/audits/2026-09-22-incident-mode-B.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-B.md`
