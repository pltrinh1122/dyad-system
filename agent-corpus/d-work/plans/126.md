# Plan #126 — bump dyad/VERSION for #114's core-craft change

Base commit of `main`: 6fb64e1. Refs #114 (whose merge caused this), #91 (the d-work that added
property 4's converse and the drift guard that caught it).

## Intent as read
No Operator prompt: this row opens from an incident found in the clerical tail of #114's Done-`Y`.
Merging #114 left `main` red. `dyad check` on `6fb64e1`:

    FAIL [infra/bundle] 'dyad-operator': dyad/ differs from tag dyad-operator-v0.8.4 (6 file(s))
    while VERSION is still 0.8.4 — bump VERSION

The six are exactly #114's core-craft files: `rules/RULE-11-distribution-structure.md`,
`vocabulary/VOCABULARY.md`, `falsification/rules/rule-11-distribution-structure.md`,
`scripts/package.py`, `tests/test_package.py`, `tests/test_craft.py`. Nothing else drifted; every
other component is clean. This is Rule-11 property 4's converse working exactly as #91 built it —
"once a craft's tag exists, the tree under its root at HEAD is the tree that tag holds, or
`VERSION` has moved" — and #114's plan named a craft bump and a bundle row but never the core's own.

## Mutation
- **Agent zone, one PR:** `dyad/VERSION` 0.8.4 → **0.9.0**.
- **Infra zone, one PR:** `BUNDLE.md` `dyad-operator` row → 0.9.0, bundle version 0.10.6 → **0.11.0**.
  Sequenced second; `infra/bundle` stays red across the gap, as with #114's own two halves.

**Minor, not patch.** #114 added surface to the core craft, not a correction to existing surface: a
new Rule-11 property-2 clause, a new Agent-vocabulary term (`bundled craft`), and two new public
functions in `package.py` (`bundled_crafts`, `release_roots`). A receiving system can now depend on
behaviour 0.8.4 does not have, which is what a minor says and a patch denies. The bundle follows to
0.11.0 for the same reason — its own version is independent of the core's (property 7) but it names
a component whose surface grew.

**Not in this mutation, stated:** no release tag is cut for either number; `dyad-operator-v0.9.0`
and `v0.11.0` would each need their own `Y/N: release <tag>?` (Rule-11 Ratification events), and
this plan asks for neither. No other component's VERSION is touched — none drifted.

## Files touched
files touched: dyad/VERSION BUNDLE.md
  agent-corpus/d-work/rows/126.md agent-corpus/d-work/plans/126.md

## Falsification
| # | attack | result |
|---|--------|--------|
| V1 | Patch, not minor — nothing *behavioural* changed for an existing caller. | **refuted** — `bundled_crafts()` and `release_roots()` are new public functions and `dyad install` now writes outside `dyad/`, which is a behaviour change a receiver can observe and depend on. Patch would understate it. |
| V2 | Major — `dyad install` writing another craft's root breaks the old contract. | **survives, and is conceded as arguable** — but the old contract is Rule-11 property 2's, which this same d-work amended, and nothing an existing caller invokes changed signature or return. Minor is the honest middle; the Operator may take major instead, and the plan-`Y` is where that is decided. |
| V3 | Leave it red and bump at the next release. | **refuted** — the guard exists precisely so a drifted tree cannot sit under an unchanged VERSION, after four drifted releases in two days (#61, #67, #71, #90). Leaving `main` red normalises the alarm it was built to raise. |
| V4 | This belongs to #114, not a new row. | **refuted** — #114 is `done`, and Rule-3 never reopens a done row: a new row refs it. Its `refs` names #114 and #91. |

**Overlap (Rule-16):** touches `dyad/VERSION` and `BUNDLE.md`; no other session's presence file
declares either. **Host actions:** none.
