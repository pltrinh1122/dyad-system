# Plan #179

Intent as read: #175's tests pass only in dyad-system because they assert its own instance values
(host defaults, 15 manifest rows, `workstation-corpus` paths) against the live repo; on
countersign-system (`host-path: infrastructure`, `host-zone: infra`) 9 fail (7 core, 2 sysadmin) and
its pre-push gate refuses countersign-system #2. Make them hermetic and add the end-to-end check that
would have caught it. Operator's `Y` to "open a dyad-system d-work to make #175's live-repo tests
hermetic (plus a scratch four-zone end-to-end test, core 0.10.1, sysadmin 0.2.1, incident recorded),
then resume countersign-system #2" is this row's plan-Y.

Mutation:
- Tests (agent zone): `test_dyadlib.HostPathTests.test_live_repo_keeps_the_default`,
  `test_containment.HostRowTests.test_zones_cli_unchanged_for_defaults`,
  `test_manifest.InstanceContributionTests.test_live_union_is_unchanged_in_content`,
  `test_crafts.CraftGuardTests.test_live_sysadmin_craft_passes`, `test_runbook.RunTests.test_cli`,
  `test_package…base_ref_does_not_resolve` (fails only through the others' guards), and the two
  references tests — each either pins the defaults (`DYAD_HOST` / `DYAD_HOST_ZONE`) or derives its
  expectation from the live repo's preferences. One new test: a scratch repo with
  `host-path: infrastructure`, `host-zone: infra` runs the guards end to end green.
- Tests (craft zone): sysadmin `test_changelog.ReferencesContribTests` (2), same treatment.
- `INCIDENTS.md` row (#175 shipped instance-bound tests); core 0.10.0 → 0.10.1, sysadmin 0.2.0 →
  0.2.1, bundle 0.12.0 → 0.12.1.
- PRs: agent (core tests + VERSION + incident), craft (sysadmin tests + VERSION), infra (BUNDLE.md),
  stacked, merged consecutively.
- Verification: the full suite green in dyad-system AND in a copy of countersign-system's seed branch
  with the new core/crafts installed.

Files touched: dyad/tests/**, dyad/VERSION, agent-corpus/audits/INCIDENTS.md, crafts/sysadmin/tests/**,
crafts/sysadmin/VERSION, BUNDLE.md.

Base commit: 2f715d1.

Falsification:
| # | attack | result |
|---|---|---|
| H1 | the tests are right and countersign-system is misconfigured | Refuted: `host-path`/`host-zone` are #175's own feature; a gate that fails on its intended use is the defect |
| H2 | skip these tests on non-default systems | Refuted: skipping a test to get green is never allowed; pinning the inputs keeps every assertion |
| H3 | this is #176 (authoring suite at the operating gate) | Survives as related, not the same: a hermetic test is required wherever it runs; #176 decides where it runs |
| H4 | one scratch end-to-end test is enough to prevent recurrence | Survives, scoped: it covers the host-zone path; the general lesson (no live-instance assertions) is the incident's |
