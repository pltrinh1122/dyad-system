# Plan #127 — is 'cos' craft part of core release?

Base commit of `main`: 0d7e2f0.

## Intent as read
A factual question about repo state: whether a craft named `cos` ships in the core release.

## Finding
No. `crafts/` in this repo holds exactly three Tended crafts — `sysadmin`, `sysarch`, `syseng`
(confirmed: `ls crafts/`, `crafts/REGISTRY.md`, `BUNDLE.md`'s four rows — `dyad-operator`,
`sysadmin`, `sysarch`, `syseng`). `sysadmin` is the one **bundled craft** (Rule-11 property 2,
added 2026-09-22 by d-work #114, a peer session's): `dyad build`/`dyad install` carry it so a
core-only install still has it. No craft named `cos` exists anywhere in this tree, bundled or not.

Every mention of `cos` in the corpus (`git grep -in '\bcos\b'`, 4 hits, all prose) is historical:
`agent-corpus/falsification/extensibility.md` and `agent-corpus/d-work/{plans,provenance,rows}/99*`
(d-work #99, 2026-09-20) name `cos` as a craft authored in a *different* repo ("`dsys-repo`'s
`cos`"), cited only as an example of an externally-authored craft hitting the core's extensibility
limits within days — never a craft in this repo, never proposed for bundling.

## Mutation
None beyond the ledger — this d-work's output is the answer above. No PR, no corpus file outside
`agent-corpus/d-work/`.

## Falsification
Claim: "cos" refers to a craft that exists somewhere in this tree. Refuted directly — `ls crafts/`
lists exactly `sysadmin`, `sysarch`, `syseng`; a repo-wide case-insensitive word-boundary grep for
`cos` finds only the four historical, cross-repo mentions above, none inside `crafts/`.

## Files touched
agent-corpus/d-work/rows/127.md; agent-corpus/d-work/plans/127.md; agent-corpus/d-work/provenance/127.md
