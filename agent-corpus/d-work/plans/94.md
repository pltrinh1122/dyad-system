# Plan #94 — adopt from ponytail into syseng (contingent; opened by #93's prompt)

Base commit of `main`: e9ef04e. Refs #93 (the evaluation and proposal:
`agent-corpus/audits/2026-09-18-ponytail.md`), #91 (syseng `VERSION` bumps on the first change).

Backlog, opened by the third line of #93's prompt ("Add to backlog an adoption plan contingent on
my review and disposition of the proposal"). Not a plan yet: the mutation is whichever of P1–P7 the
Operator keeps when disposing the proposal; this file is rewritten from that disposition before any
plan-`Y` is asked.

## Intent as read
Bring into the syseng craft the items of the proposal's Part 2 that survive the Operator's review.

## Mutation (outline; Part 3 of the audit is the source)
1. `crafts/syseng/rules/verifiable-code.md`: the ladder property with its never-simplify floor (P1,
   P2) and the check sentence (P3, keeping the test mapping unless the Operator loosens p4);
   `vocabulary/CRAFT.md` terms; falsification amendment; `VERSION` 0.1.4 → 0.2.0.
2. `naming.md`: the `ceiling:` marker (P4a); its guard and test (P4b).
3. `templates/simplify-review.md` (P5).
4. `preferences-corpus/PREFERENCES.md`: `implementation-minimality` (P6) — preferences zone.
5. `crafts/syseng/scripts/bench.py` + test (P7); a first measured run as its own d-work with a cost.
6. Optional: reproduce ponytail's agentic benchmark (~$15–25, a credential) — its own d-work.
Each step one craft-zone PR (4 is preferences zone), one Done-`Y` each or batched.

## Files touched
To be named from the disposition.

## Falsification
Deferred to the rewrite; the proposal's own attacks are in the audit, Part 1.4 and Part 2 (P3's
tension with the test mapping is the decision the Operator owns).
