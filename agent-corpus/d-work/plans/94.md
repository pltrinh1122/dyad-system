# Plan #94 — adopt from ponytail into syseng: the falsified survivor

Base commit of `main`: 5fdb6ae. Refs #93 (evaluation and proposal), #95 (P3/P4 decision framing,
scored), #91 (the drift guard: `VERSION` bumps below are load-bearing, not ceremony).

## Intent as read
"You have everything you need … falsify. implement survivor." Not an enumerated P1–P7 disposition:
the Operator delegates the selection to falsification, and names the shape — "some," not all. #95
already scored P3 → A and P4 → P4d; this plan falsifies P1, P2, P4's *mechanism* (not its shape,
already decided), P5, P6, P7 for whether each is implemented **now**, applying the proposal's own
rung 1 to itself: does this need to exist yet?

## Falsification (survivor per item)
- **P1 (the ladder as a chosen-path property) + P2 (never-simplify floor) + P3 (check floor, A).**
  Attack: "rule text with no guard is not really adopted." Refuted: most of `verifiable-code.md`'s
  existing properties are inference-checked (Enforcement: "whether a test is adequate is inference
  at falsification time"); this craft's own pattern is text that later PRs give a guard when one is
  warranted, not text that waits for one. **Survives — implement now**, one property, three clauses.
- **P4, the mechanism.** #95 decided the *shape* (P4d: a marker cites a backlog row) but not
  *when to wire it into Rule-20*. Attack, applying rung 1 to the proposal itself: does a whole-tree
  comment scanner (a new `REFERENCES` extractor reading arbitrary source comments across every
  tracked file, `.py` and `.md`, with its own false-positive shape) need to exist *this turn*, or
  can the convention be adopted and the enforcement deferred? The scanner is real new code in the
  agent zone (`dyad/guards/agent/references.py`), cross-zone from the craft convention that
  triggers it, and its own correctness (what counts as a "ceiling comment," across languages) is
  undesigned. Refuted as "implement now": disproportionate to bundle into a pass the Operator
  scoped to "some." **Survives, split: the marker *form* is adopted now** (`naming.md` row,
  `checkable: no` — free prose until registered, Rule-20 Boundaries' own exclusion: "a mention …
  is not a reference"); **the Rule-20 wiring is named and deferred**, its own backlog row, `refs:
  94`, so it is not lost and is not rushed.
- **P5 (tagged one-line review template).** Attack: "redundant with the session's own `/simplify`
  skill." Refuted as a reason to drop it: `/simplify` is a harness-level tool with no fixed output
  grammar; this is a project template a completion reply can cite by path, the same relationship
  ops-scripts have to ad hoc shell commands. Zero new code (a template file). **Survives —
  implement now.**
- **P6 (an `implementation-minimality` preference).** Attack, ponytail's own rung 1: does a dial
  need to exist before any instance has asked to be at a different setting than `full`?
  **Refuted as premature — not implemented.** Named in the audit as available if a real need
  appears; adding it later is one preferences-zone PR, not a design debt.
- **P7 (the benchmark harness).** Attack: "the most transferable item, drop it and the proposal's
  best idea is lost." Survives as a *proposal*, refuted as "implement now": it needs a task set, a
  scorer, and a credentialed run (Rule-8, Rule-14) — three independent design decisions this plan
  cannot make for the Operator in the same breath as P1–P5's rule text. **Not implemented; its own
  backlog row**, `refs: 94`, distinct from P4's wiring row.

Survivor set implemented in this d-work: **P1 + P2 + P3 + P4-form + P5**. Not implemented, each
its own named backlog row: P4-wiring, P7. Not adopted: P6.

## Mutation
- **Craft zone, one PR.** `crafts/syseng/rules/verifiable-code.md`: property 7, "The path is the
  first rung that holds" — (1) needs to exist? (2) already in this codebase? (3) stdlib? (4) native
  platform? (5) an installed, declared dependency (`imports.md` p1–2)? (6) one line? (7) the
  minimum that works — run after the plan has read the code the change touches; bounded by "never
  simplify away a trust-boundary check, error handling that prevents data loss, security or
  accessibility, or anything requested" (P2); and "code without its check is unfinished — the
  smallest check that fails when the logic breaks is enough inside the test the mapping already
  requires" (P3, property 4 gains one clause, the mapping itself untouched).
  `crafts/syseng/rules/naming.md`: one table row, `` `# ceiling: <what is bounded>; #<row>` ``,
  names a deliberate scope ceiling and the backlog row tracking when to lift it, owner Rule-13
  (a ceiling is a scoped decline of rung 1), `checkable: no` (inference; #94's wiring row may make
  it `yes`). `templates/simplify-review.md`: the tagged-finding format (`delete:|stdlib:|
  native:|yagni:|shrink:`, one line per finding, `net: -N lines possible`). `vocabulary/CRAFT.md`:
  `ladder`, `ceiling` (owner the new property / naming row). Falsification record amendment in
  `crafts/syseng/falsification/rules/verifiable-code.md`, citing #93/#95's tables rather than
  repeating them. `VERSION` 0.1.4 → 0.2.0 (a new property and a new naming-table row are new
  surface, not correction — the #91/#67 distinction).
- **Infra zone, one PR, after craft.** `BUNDLE.md`: `syseng | 0.2.0`, bundle version bumped.
  Throwaway-merge `check --evidence` before the Done-`Y`; merged after `dwork state 94 done`.
- **Two backlog rows**, opened in the same ledger-only commit as this plan's own open, each
  `refs: 94`: (a) "wire ceiling markers into Rule-20 (P4 mechanism): a references.py extractor,
  new kind, tests"; (b) "ponytail benchmark harness for syseng (P7): task set, scorer, first
  measured run (credential, cost)."
- Release (`syseng-vX.Y.Z`, bundle) asked separately after Done-`Y`, as always.

## Files touched
crafts/syseng/rules/{verifiable-code,naming}.md; crafts/syseng/templates/simplify-review.md;
crafts/syseng/vocabulary/CRAFT.md; crafts/syseng/falsification/rules/verifiable-code.md;
crafts/syseng/VERSION; BUNDLE.md; agent-corpus/d-work/{rows,plans,provenance}/94.md and the two
new backlog rows'.

## Falsification, the mutation itself
- "One property is doing three jobs (ladder, floor, check) — split it." Refuted: the three read as
  one sentence in the proposal ("run the ladder, never past the floor, never without the check")
  and `verifiable-code.md`'s existing properties are similarly compound (p1 pairs "code over
  inference" with what stays inference).
- "The naming-table row without a guard invites drift (someone writes `# ceiling:` some other
  way)." Survives, scoped: exactly the drift the wiring row (a) exists to close; until then it is
  documentation, the same footing as several existing `checkable: no` rows in the same table.
- Presence: no session names `crafts/syseng/…` or `BUNDLE.md`. Host actions: none.
