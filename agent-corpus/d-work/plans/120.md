# Plan #120 — incident mode D: wrong cause asserted then retracted (2 incidents: the v0.3.2 403, the 755 modes)

Base commit of `main`: 1dd364f. Refs parent #116 (the audit that classified the log; its plan holds the full table).

## Intent as read
One d-work per observed failure mode (Operator's N on #116). This one owns mode D: its
definition, its incidents from `agent-corpus/audits/INCIDENTS.md`, and a falsified remediation
candidate — surfaced, not disposed. Remediation, if wanted, is the Operator's prompt on this row.

## Incidents (mode D)
#13/#16 (the v0.3.2 release 403 blamed on default_workflow_permissions; persisted after the change; still open); #6 (755 modes blamed on git add on this mount; the seed archive was the cause)

## Falsification (candidate remediation)
Both cases stated a cause as fact before an experiment separated it from alternatives; one is still open (the 403). Candidate survivor: a cause in a completion reply or incident row is written as 'hypothesis' until one observation would have differed under the alternative -- conduct, no mechanism. Attack: unfalsifiable as a guard -- conceded and stated.
- Rule-16 overlap: no presence file names `agent-corpus/audits/`; peer branches are disjoint.

## Mutation (what a plan-`Y` authorizes)
One agent-zone PR (branch `agent/120-mode-D`): `agent-corpus/audits/2026-09-22-incident-mode-D.md`
— definition, the incidents above with date and d-work, the candidate and its attacks. Nothing
else: no Rule, guard or craft file; no `VERSION`; no release.

## Files touched
`agent-corpus/audits/2026-09-22-incident-mode-D.md`

## Amendment — 2026-09-22 (naming correction)
The record's file name is `agent-corpus/audits/2026-09-22-incident-mode-d.md`, lowercase:
`syseng/naming` requires the slug `[a-z0-9-]+` under `<instance>/audits/`, which the uppercase
mode letter this plan first named fails. Caught by the combined `check --evidence` run over all
ten branches before any merge; corrected in a second commit on this d-work's branch.
