# Plan #104 — workstation-252: dyad project --list's column format is a downstream compatibility surface

Base commit of `main`: 3f3ca32. Refs workstation-252 (via `dsv0.5.1: workstation`, 2026-09-20,
message 3 report 1), #100 (D3, the `craft/surface` rekey that changed this format).

Prompted 2026-09-20 (Operator `Y` to the batched proposal covering #92, #103, #104): add a
documentation note (not a defect — the format change was intentional and already documented;
workstation's own `surfacer` craft already absorbed it on their side, no dyad-system code change
requested or needed).

## Intent as read (the sender's observation, verbatim as carried)
`dyad project --list`'s output changed from a one-column to a `<craft/surface> <path>` two-column
format (D3, this session's own #100 PR2/PR3 work, keying the projector registry `craft/surface`
instead of bare `surface`). Workstation's `surfacer` craft parsed the old one-column format and
broke; they fixed it on their own side (commit `f39fdf8`, per their report) and are surfacing this
only so other CLI-output consumers know the shape changed. No dyad-system defect: the change was
the falsified, ratified survivor of #100 D3, and `projection.md` property 4 already documents the
new format (`dyad project --list` prints `<craft/surface> <path>`).

## Mutation
Craft zone (sysarch), one PR (with #103): `crafts/sysarch/rules/projection.md` property 4 gains
one sentence, immediately after the `dyad project --list` format sentence, naming the line as a
compatibility surface for tooling outside this repo, so a future change to its columns or
separator is falsified with that downstream cost in view rather than treated as a local
formatting choice. `crafts/sysarch/VERSION`: 0.2.0 → 0.2.1 (shared bump with #103, one content
change to the craft's tree).

Infra zone, a second PR after the craft PR merges (with #92, #103): covered by #103's `BUNDLE.md`
row (one sysarch VERSION bump serves both rows in this plan).

Release: covered by #103's `sysarch-v0.2.1` and the shared bundle `v0.10.1` (no separate tag —
one craft, one VERSION, one release per craft per Rule-11 property 4).

## Files touched
`crafts/sysarch/rules/projection.md` (shares the PR and the VERSION bump with #103).

## Falsification
Intake shape: defect-shaped report, but its own content states no dyad-system defect exists —
treated as a documentation prompt, not a fix; admissible on the same footing as a defect intake
(observation with evidence, no verdict imposed, the Operator disposes what to do with it).

Attack: the note is optional prose with nothing to check mechanically — inference-only, exactly
like the rest of `projection.md`'s "Inference, stated" section. Result: accepted as scoped — this
is a documentation d-work by design; Rule-9 falsification of a documentation claim is whether it
is *true* (the line is in fact machine-parsed downstream — confirmed by workstation's own report)
and *stated once, not enforced twice* (no new guard; property 4 is Tended prose, checked by
inference like the properties around it).