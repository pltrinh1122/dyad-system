# Provenance #18

## 1 prompt 2026-09-15 [credential redacted]

```
[the Operator pasted a GitHub fine-grained PAT here, in the clear, as the value for the RELEASE_TOKEN
Actions secret discussed in the prior reply's AskUserQuestion ("PAT secret" option). The raw token is
deliberately not transcribed into this corpus (Rule-7 property 4 yields to not committing a live
credential to git history, even one already exposed in the session transcript by the Operator's own
message). It was used once, via stdin, to run `gh secret set RELEASE_TOKEN --repo
pltrinh1122/dyad-system --body -`, confirmed by `gh secret list`, and the Agent recommended the
Operator rotate it since it now lives in the session transcript outside the corpus's control.]
```
