# Provenance #178

## 1 prompt 2026-09-25
```
the current chat surface between agent-operator falls under `ui-design` or `ux-design`.  falsify.
```

## 2 disposition 2026-09-25
```
N: not `ui-design` but `ux-design`. falsify.
```

## 3 disposition 2026-09-25
```
N: what is in the `core` craft?  right now, there's `sysarch`, `syseng` and `sysadmin`.  so the Operator's experience with the chat (CLI) interface with Agent is `sysux` (or `sysdialog`).  falsify.
```
