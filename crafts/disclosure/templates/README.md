# disclosure craft templates

No template yet. The pseudonym map and the summaries are seeded by the d-work that first produces a
disclosure, not by this craft: both are instance state, and a template that shipped an empty map would
invite a second map beside the real one.
